using System.Runtime.CompilerServices;

// Відкриваємо internal-члени ядра тестовій збірці, щоб тести могли
// виставляти контрольований стан (напр. InjuryPoints) напряму,
// не розширюючи публічний API гри.
[assembly: InternalsVisibleTo("Game.Tests.EditMode")]

// Симуляційний харнес читає заряди накопичувачів і значення Напруги, щоб
// вивантажувати трасу кампанії. Це інструмент дизайнера, що живе в tools/ і
// збирається лише headless; збірка Game.Gameplay такого доступу НЕ отримує,
// тому дашборд у самій грі, як і раніше, неможливо зібрати (інваріант 3).
[assembly: InternalsVisibleTo("Alpha.Sim")]
