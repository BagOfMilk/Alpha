using System.Runtime.CompilerServices;

// Открываем internal-члены ядра тестовой сборке, чтобы тесты могли
// выставлять контролируемое состояние (напр. InjuryPoints) напрямую,
// не расширяя публичный API игры.
[assembly: InternalsVisibleTo("Game.Tests.EditMode")]

// Симуляционный харнес читает заряды накопителей и значение Напряжения, чтобы
// выгружать трассу кампании. Это инструмент дизайнера, живущий в tools/ и
// собираемый только headless; сборка Game.Gameplay такого доступа НЕ получает,
// поэтому дашборд в самой игре по-прежнему невозможно собрать (инвариант 3).
[assembly: InternalsVisibleTo("Alpha.Sim")]
