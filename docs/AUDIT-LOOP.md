# Аудит главного лупа (31.08.2026)

> **Статус документа:** отчёт аудита. Источником истины НЕ является —
> при конфликте: поправки > GDD > `SETTLEMENT_LAYER.md` > этот файл > код.
> Задача документа — держать найденные разрывы на виду, пока они не закрыты.

## Что уже починено

Пять неисправностей из раздела «Рекомендация, шаг 1» закрыты коммитом
«Этап −1». Каждая закрыта вместе с тестом-охранителем в
`Assets/Tests/EditMode/LoopRepairTests.cs`, и каждый охранитель проверен
мутацией: если вернуть старое поведение, тест падает.

| Неисправность | Что сделано | Охранитель |
|---|---|---|
| Кризис не срабатывал никогда | Ворота «объявлено и услышано» переехали в `PressureTrack.IsReady`, поэтому заряд больше не сгорает впустую. `CrisisGraceDays` переехал в `PulseBalance` — к своему единственному потребителю | `Crisis_OnFractureBand_ActuallyFires`, `Crisis_IsPrecededByHeardLevel3AndGraceWindow` |
| Календарь считал фазы | `CurrentDay` растёт только на дневной фазе; фоновый тик Напряжения стал суточным | `Calendar_FullDay_AdvancesCounterOnce`, `Calendar_TierTick_AppliedOncePerDayNotPerPhase` |
| Проспанный предвестник сгорал | Ступень засчитывается только УСЛЫШАННАЯ: `WorldPulse.MarkDelivered` вызывает шаг дня, который один знает, спал игрок или патрулировал | `Forewarn_SleptThrough_IsOfferedAgainLater` |
| Лестница предвестников вырождалась | `Fire()` разряжает накопитель полностью, а не переносит остаток | `Forewarn_LadderNeverSkipsAStep` |
| Предвестник называл чужой домен | У инцидента появился `SourceId`; отбор идёт только из пула сработавшего накопителя | `IncidentStep_ResolvesIncidentOfTheSourceThatFired`, `Incidents_AreDrawnOnlyFromTheirOwnSource` |

Заодно переписаны три теста, которые проходили вакуумно:
`Assert.Pass("Кризис не наступил")` заменён проверкой порядка; инвариант
«не меньше трёх накопителей» теперь меряется по всему пространству
состояний, а не в единственной точке, где он выполняется; пассивная
кампания гоняется полными сутками и на всех четырёх тирах.

Два разрыва зафиксированы тестами НАМЕРЕННО, как известные и не
закрытые — чтобы они падали в CI, когда до них дойдут руки:
`Pulse_ActiveTrackCount_AcrossWholeStateSpace` (инвариант держится в 2
состояниях из 10) и `Tension_PassiveCampaign_HighTiers_DoReachFracture_KnownGap`
(на тирах 3-4 фоновый тик сам доводит город до кризиса).

## Что НЕ починено

Всё остальное из этого отчёта. Ближайшее по плану — шаг 2
(симуляционный харнес и контракт сейва) и шаг 3 (одно решение в сутки).
Три вопроса из последнего раздела ждут решения владельца; без ответа на
третий (откуда берётся материал для городских проектов) направление А
писать нельзя.

---

# Аудит головного лупа

## Що в нас за луп насправді (коротко, з посиланнями на файли)

Головний луп оголошено міським (Поправка №3.1), і в коді він справді існує — але як **конвеєр обчислень без жодної точки введення**.

**Доба = виклик `DayProcessor.Advance(DayPhase)`** (`Assets/_Project/Scripts/Core/Loop/DayProcessor.cs:76-99`). Усередині: `CurrentDay++` → `_tension.BeginDay()` → цикл кроків → `DayReport`. Календарна доба збирається з ДВОХ таких викликів (`DayProcessor.cs:113-116`, `AdvanceFullDay`).

**Порядок кроків** зафіксовано в `Assets/_Project/Scripts/Core/Loop/DayStepOrder.cs` — 12 позицій:
`Clock 0 → Construction 100 → Production 200 → Population 300 → Derived 400 → Tension 500 → Obligations 600 → Pulse 700 → Incidents 800 → Healing 900 → Signals 1000 → Report 1100`.

**Реалізовано з них чотири.** `DayProcessor.DefaultSteps()` (`DayProcessor.cs:66-75`) повертає рівно `TensionTickStep, PulseStep, IncidentStep, SignalStep`; grep `: IDayStep` по всьому репозиторію дає ті самі чотири класи. Clock і Report обслуговуються всередині `Advance`. Construction/Population/Derived/Obligations помічені `[ПОЗЖЕ] Э3/Э4` — це план. **Production=200 і Healing=900 позначки [ПОЗЖЕ] не мають і при цьому не підключені** — це діри.

**Три джерела тиску** (`Assets/_Project/Scripts/Core/World/DefaultIncidents.cs`): `StreetPressureSource` (`:146`, `IsActive => true`), `NightPressureSource` (`:163`, `IsActive => ctx.IsNight`), `CrisisPressureSource` (`:185`, `IsActive => TensionBandIndex >= 3`). Інших реалізацій `IPressureSource` у продакшн-коді немає.

**Стан гравця в добі:** `Tier` — публічна властивість, яку ніхто, крім інспектора демки, не змінює (`DayProcessor.cs:29-31`); `IsPatrolling` — булевий прапорець; призначення на пости живуть у `Core/Base/BaseState.cs` (`TryAssign`) і робляться один раз до циклу. Виробництво крутиться окремим годинником: `BaseState.AdvanceCycle()` з власним `CurrentCycle`, який `DayProcessor` не викликає жодного разу.

**Тестів** — 93 `[Test]` у 13 файлах `Assets/Tests/EditMode/` (CLAUDE.md каже 91; розбіжність несуттєва). Вони стережуть інваріанти й обмеження.

Практично: гравець розставляє людей, ставить галочку «патрулювати», і далі 300-400 разів натискає «далі», читаючи `SignalDigest`. Демка так і зветься — `RunSimulation()`.

---

## Діагноз одним абзацом

Ми збудували якісний, дисциплінований і повністю детермінований **симулятор**, який не є **грою**: у добі дванадцять кроків обчислення і нуль кроків введення, тому все, що система вміє порахувати наперед — виконавця, підхід, поріг, наслідок — вона рахує замість гравця. Поверх цього три накопичувачі виявилися трьома обгортками однієї прихованої змінної; лічильник часу рахує фази замість діб, через що всі якорі темпу помилилися рівно вдвічі; кульмінація, заради якої побудовано п'ять полос, три ступені предвісників і grace-вікно, **у коді не може спрацювати ніколи** (`Fire()` обнуляє `AnnouncedLevel` до того, як його читає ворота кризи); а єдиний канал знання про світ повідомляє стан, ніколи не повідомляє причину, повторює чотири незмінні ключі щодня і не дає перечитати вчорашнє. Найгірше — це не окремі баги: **дві найдорожчі підсистеми (телеграфія і призначення) з'єднані з рештою гри розірваними дротами**, тому їх не видно ні в плейтесті, ні в тестах, які проходять вакуумно (`Assert.Pass("Кризис не наступил за 120 дней — тоже валидный сценарий")`).

---

## Проблеми за темами

Серйозність: **К** — критична, **С** — серйозна, **П** — помірна.

### Тема 1. Доба без ходу: гравець не приймає рішень

| Проблема | Сер. | Чому це станеться (механічно) | Референс, який це вже проходив |
|---|---|---|---|
| `no-player-verb-inside-the-day` — у добі немає точки рішення | К | `IncidentStep.cs:31-36` робить `Pick` і одразу `IncidentResolver.Resolve` в одному циклі. `DayContext` не має ані черги пропозицій, ані прапорця паузи. Демка задає «рішення гравця» константним масивом `heavyChoiceDays = { 8,18,28,... }` | Dragon Age: Inquisition (воєнний стіл), Massive Chalice («середня ланка менеджменту, що робить призначення, які комп'ютер зробив би швидше») |
| `incident-has-no-decision-point` — «моральна криза» приходить протоколом постфактум | К | `IncidentResolver.cs:49-56` безальтернативно будує `CheckRequest` із `QuietPath*`. Гравець не обирає ані підхід, ані виконавця, ані чи втручатися | Frostpunk тримається на підписанні закону; CK3 — на модальному вікні події |
| `patrol-is-free-dominant` — патруль безкоштовний і строго домінантний | К | Кроку `Healing(900)` не існує; втоми в `DayContext` немає. `IsPatrolling=true` ділить нічну ставку на 3 (`DefaultIncidents.cs:172-177`) і відкриває ВСІ нічні предвісники (`PulseStep.cs:27-30`) — за нуль | RimWorld-моди: спільнота першою автоматизує те, що ніколи не було вибором |
| `quest-choice-band-change-is-mute` — вибір у квесті змінює полосу НІМО | К | `TensionDrivers.QuestChoice` пишеться ЗЗОВНІ конвеєра; наступний `Advance` першою дією робить `_tension.BeginDay()` → `_dayLedger.Clear()` (`TensionState.cs:77-80`). Кандидата `tension.band.X` композитор не отримує. Тест обходить конвеєр, демка компенсує власним друком (`SettlementChronicleDemo.cs:110-113`) | CK3/Frostpunk: наслідок підтверджується тим самим тиком |
| `assignments-disconnected-from-incidents-in-slice` — призначення не впливають ні на що | С | Інциденти адресують `settlement_market`/`council_seat`/…, а демка і смоук-тест створюють слоти `"watch"`/`"market"`. За 90 днів жоден інцидент не бачить призначеного напарника. Тесту на існування `RelevantPositionId` у каталозі немає | Frostpunk/RimWorld: коли результат не залежить від призначень, гравець перестає керувати |
| `bloody-path-dead-data` — друга гілка інциденту заповнена і не читається | С | `BloodyPathSkill`/`BloodyPathThreshold` оголошені (`IncidentDefinition.cs:49-50`) і заповнені у 7 інцидентах; у `IncidentResolver.cs` слово `BloodyPath` не зустрічається жодного разу | Deus Ex: HR, Dishonored продають цю вісь чесно — летальний шлях справді коротший |
| `waiting-is-free-dominant` — «чекати» пропускає ночі цілком | С | `Advance(int days, DayPhase phase = DayPhase.Day)` (`DayProcessor.cs:101-107`) крутить лише денні фази. Нічний накопичувач не набирає нічого, фоновий тік удвічі менший — усупереч docstring `AdvanceFullDay` «ніч не можна пропустити» | Against the Storm свідомо ввела Нетерплячість Королеви, бо без ціни за час гравець обирає найбезпечніший темп |
| `wait-does-not-interrupt` — прокрутка не має контракту переривання | П | `for (int i=0;i<days;i++) reports.Add(Advance(phase)); return reports;` — жодної умови зупинки, жодного повернення фактичної кількості діб. Предвісник видається лише в момент підвищення ступеня і згорає всередині пачки | Стандартний контракт «швидкої перемотки» в менеджментах: зупинка на першій події |

### Тема 2. Телеграфія, що не доходить або бреше

| Проблема | Сер. | Чому це станеться (механічно) | Референс |
|---|---|---|---|
| `crisis-gate-self-defeating` — **кризи в грі не існує взагалі** | К | `WorldPulse.Advance` оголошує предвісник (`:74-77`), потім у ТОМУ Ж виклику `Fire()` (`:172-176`), а `Fire` останнім рядком робить `AnnouncedLevel = 0` (`PressureTrack.cs:67-70`). `IncidentStep.cs:25` читає `AnnouncedLevelOf(sourceId) < 3` — умова істинна ЗАВЖДИ, `continue` спрацьовує завжди. `crisis_riot` не доходить до резолвера ніколи. Тест проходить вакуумно (`NightAndReportTests.cs:184` `Assert.Pass`). Додатково: `CrisisGraceDays = 3` оголошено і не читається жодним рядком | XCOM 2 (Avatar), Frostpunk (Велика Буря): смуга, що заповнюється й ніколи не спрацьовує, вчить ігнорувати попередження |
| `forewarning-lost-forever-when-asleep` — ступінь втрачається безповоротно, і ворота кризи читають недоставлений рівень | К | `WorldPulse.cs:73-78` піднімає `AnnouncedLevel` беззастережно; `PulseStep.cs:28-30` лише потім вирішує `canHear = !IsNight \|\| IsPatrolling`. Умова видачі — `level > AnnouncedLevel`, тож ступінь не повториться до наступного `Fire()`. Ворота кризи читають ДОСЯГНУТИЙ рівень — тобто криза може вдарити після предвісника, якого гравець не чув | Порушує власне §5.1 правило 4 і мітигацію ризику N6 |
| `forewarn-ladder-degenerates` — після удару місто одразу знову кричить «Скоро» | К | Стеля заряду `Threshold × 2`, `Fire` віднімає лише один `Threshold` і обнуляє `AnnouncedLevel`. Насичений трек наступної ж фази знову оголошує рівень 3 — без сходинок 1 і 2. Для кризи найгірше: поріг 120, ставка 12, `CooldownDays=30` — місто щодня каже «ось-ось», хоча удар фізично неможливий ще 30 днів | §5.1 «Предвестник не врёт» ламається дослівно |
| `forewarn-domain-mismatch` — домен предвісника не є фільтром відбору інциденту | С | `IncidentTable.Eligible` (`:31-39`) фільтрує лише `IsCrisis`, `MinBand/MaxBand`, `MinTier`, `NightOnly`. Домени треків: «улицы»/«ночь»/«площадь»; домени інцидентів: «склад»/«рынок»/«припасы»/«лазарет»/«окраина». На Спокійно вдень активний лише трек «улицы», а `Eligible` віддає `spoiled_stores` («припасы») | Ціна механічна: у кожного інциденту є `RelevantPositionId`, і невірний домен знецінює саме ту контргру, заради якої телеграфія існує |
| `signal-budget-evicts-telegraphy` — зміна полоси може бути німою | С | `ForceSignalOnBandChange` лише ДОДАЄ кандидата (`SignalComposer.cs:118-131`), слота не резервує; полосам Ропот/Брожение `UrgencyForBand` дає лише `Notable`. День із кризою (`Imminent`) і трьома `Alarming` викидає зміну полоси — прямий злам інваріанта 4. Плюс завжди вилітають `forewarn.level1` і `tension.ambient.*` (обидва `Ambient`, обидва не-дельти) | CK3: гравці ставлять моди на зменшення спаму і клікають події не читаючи |
| `signals-are-one-shot-no-journal` — єдиний канал знання доставляється один раз | С | `SignalDigest` живе рівно один `DayReport`; журналу немає ні в `Core.Signals`, ні серед екранів Эпіка 17. При бюджеті 4 на фазу повз гравця проїжджає до 8 сигналів на добу | Cultist Simulator: відмова в журналі заради «нервозності» — найчастіша претензія до гри |

### Тема 3. Час коштує нічого, а лічильник часу бреше вдвічі

| Проблема | Сер. | Чому це станеться (механічно) | Референс |
|---|---|---|---|
| `tension-tick-doubled-by-phase` — **усі якорі темпу помилилися вдвічі** | К | `Advance(DayPhase)` робить `CurrentDay++` на КОЖНУ фазу; `TensionTickStep` не має перевірки фази. Вікна в днях міряються тим самим лічильником: `CooldownDays` street 4 → 2 доби, crisis 30 → 15; `RepeatWindowDays` 7 → 3.5. Якір «хутір не доходить до кризи за кампанію» ламається арифметично: тир 2 × 200 діб × 2 фази × 2.0 = 800 = рівно Излом. Продакшн-код це вже знає: `SettlementChronicleDemo.cs:98` друкує `report.Day / 2 + 1` | — |
| `passive-drift-doom-clock-at-high-tiers` — тири 3-4 ніхто не перевіряв | С | `TensionStateTests.cs:61` — `foreach (int tier in new[] { 1, 2 })`, і `p.Advance(200)` = 200 фаз = 100 діб. Гарантія міряє півкампанії й лише низькі тири. На тирі 3 це 8/добу (Излом за 100 діб), на тирі 4 — 14/добу (за ~57) | RimWorld: складність від багатства → «карає за ріст»; XCOM 2 у WotC додали опцію подвоїти таймер |
| `no-save-contract-state-unrestorable` — стан шару неможливо зберегти | К | `DayProcessor.CurrentDay { get; private set; }`, `PressureTrack.Charge` — `internal … private set`, `TensionState._fraction` і `DaysInCurrentBand` приватні. Після завантаження заряди будуть нулями. Grep по `SETTLEMENT_LAYER.md` на «сохран\|сериализ\|сейв» — 0 влучень. Детермінізм дійсний лише в межах одного процесу; `Pulse_SameInputs_IdenticalSchedule` цього не бачить | — |
| `save-scum-turns-hidden-info-into-perfect-info` — сейв-скам дає точну розвідку майбутнього | К | US-16.1 дає вільні сейви в базі. При повному детермінізмі повторний прогін бітово ідентичний: зберігся, прокрутив три доби, подивився інцидент і домен, завантажився — знання абсолютне, а не ймовірнісне. Айронмен увімкнено там, де він не потрібен (вилазки), і вимкнено там, де він єдиний захист ядра | Детерміновані roguelike-режими з вільним сейвом: масовий «скаут-і-відкат» |

### Тема 4. Пульс: один накопичувач у трьох обгортках

| Проблема | Сер. | Чому це станеться (механічно) | Референс |
|---|---|---|---|
| `one-active-track-most-of-the-time` — інваріант ≥3 виконано у 2 комбінаціях з 10 | К | Перебір 5 полос × 2 фази: band 0-2 удень — 1 трек, вночі — 2; band 3-4 удень — 2; ≥3 лише Накал/Излом уночі. Тест сам обирає найкращу точку і зізнається в коментарі: «Ночь на верхней полосе — момент, когда работают все три». `MinActiveTracks` у рантаймі не читається ніде | Frostpunk: після одного проходження всі події відомі |
| `three-tracks-one-variable` — три ставки корелюють на 100% | С | street = 6+band×4, night = (10+band×5)[÷3], crisis = band≥4 ? 12 : 5. `PulseContext` має рівно 5 полів (Day, IsNight, Tier, TensionBandIndex, IsPatrolling). Хто навчився читати полосу — читає ВЕСЬ Пульс. Населення і призначення (які вже існують) на Пульс не впливають | Factorio: накопичувач наповнює сам гравець і бачить його як забруднення; RimWorld: raid points від багатства |
| `incident-selector-is-day-hash` — вибір події з дати, а не з причини | С | `selector = ctx.Day * 31 + sourceId.Length * 7 + i`. Перейменування джерела зі `"street"` на `"streets"` мовчки перетасує всю кампанію. Пам'яті теми немає: ані кулдауна, ані вигорання. Коментар `IncidentTable.cs:65-66` обіцяє участь заряду накопичувача — її в коді немає | Skyrim/Fallout 4: радіантні квести з крихітного пулу — не контент |
| `second-playthrough-is-a-replay` — реіграбельності немає структурно | С | Немає сида. Джерела фіксовані, пороги — константи, таблиця одна, спайн лінійний, селектор — арифметика від дати. Тест `Pulse_SameInputs_IdenticalSchedule` закріплює це як бажану властивість | Frostpunk: заскриптований розклад дає бездоганне перше і майже нульове друге проходження |
| `pressure-sources-have-no-author` — тиск безсуб'єктний | С | `Id = "street"/"night"/"crisis"`, домени — місця. У `IncidentDefinition` немає поля ініціатора. `SubjectId` заповнюється лише двічі (жертва кризи, держатель посту) | CK3: накопичувач загрози — завжди чиясь схема з іменем; Against the Storm персоніфікувала таймер у Королеву |

### Тема 5. Люди — числа, а не персонажі

| Проблема | Сер. | Чому це станеться (механічно) | Референс |
|---|---|---|---|
| `protagonist-covers-every-post` — один рядок вимикає стимул Эпіка 8 | К | `CheckResolver.cs:136-140`: `if (onPost \|\| a.IsProtagonist) candidates.Add(a);`. `HasCandidate` ніколи не false → гілка `Worst + wasUnmanned` недосяжна → `PostReport.Silent` недосяжний → «сліпота» Поправки 3.8 не настає ніколи. Тести проходять лише тому, що протагоніста в їхньому ростері немає. **Джерело суперечності — сам GDD.md:273**, де обидва взаємовиключні правила стоять в одному реченні | Pillars of Eternity: «лідер-соло» знецінює решту партії |
| `crisis-kills-the-one-you-assigned` — держатель поста завжди перша мішень | С | `PickVictim` (`:144-149`) спершу бере `ActorOnPosition(RelevantPositionId)`, потім падає в алфавітний фолбек `CompareOrdinal`. У зв'язці з попереднім це чистий штраф: протагоніст усе одно виграє перевірку, полоса не зміниться, а гине призначений. Плюс за однакового ростера першим гине один і той самий напарник у кожній кампанії | Battle Brothers у ранніх патчах карала за «правильне» використання персонажа |
| `injury-has-no-consumer` — поранення нічого не означає | С | Класу `HealingStep(900)` немає; `BaseState.ApplyHealing` конвеєр не викликає. 30 очок поранення не зникають ніколи. `IsPresentInSettlement` виключає лише OnMission/Dead, `Wound` не чіпає `AssignedSlotId`, `GetCheckValue` не дивиться на `InjuryPoints`. `HealingPerMedicinePoint=2.0` не читає ніхто | Battle Brothers: поранення тримають пізню гру живою через ротацію дефіцитного резерву |
| `band-ceiling-flatlines` — стеля полос пробита на нульовому дні | С | Пороги — константи в даних (5..10), `BestMargin=7`, дисперсії немає. `value ≥ threshold+7` назавжди фіксує Кращу полосу. Гірше: адаптер мапить `"intimidate"` на `StatType.Will`, а `DefaultContent` дає Will=30/35 проти порога 8 — вічна Краща полоса без вкладень. Поруч `Aim=65` і `Engineering=6` — несумісні шкали | Fallout 2, Deus Ex: HR — скіл понад стелю контенту перетворює перевірку на текст із зеленою галочкою |
| `no-port-for-companion-opinion` — напарник не має механізму мати думку | С | Специфікація §2.1 вимагає `HasValueTag(string)` у `ISettlementActor` — його в інтерфейсі немає. Лояльності немає ніде: у `Companion` лише Level/Xp/Status/InjuryPoints/AssignedSlotId. За 90 днів напарник змінює два стани: поранений і мертвий. US-9.2 позначено [ЯДРО], але вона не входить у жоден етап Э0-Э4 | CK2: думка з видимими причинами — головний двигун прив'язаності; Battle Brothers: незворотні видимі каліцтва |
| `city-voice-has-no-speakers` — у міста немає жодного імені | П | `subjectId: null` у фоновій репліці, нічному амбієнті й переході полоси. «Людність» — це `int`. `ContactState` віднесено в Э2/Э4, полегшеного варіанта в Э1 немає | Disco Elysium, Kingmaker: поселення тримається на кількох повторюваних іменах |

### Тема 6. Міста немає: конвеєр, економіка, храповик

| Проблема | Сер. | Чому це станеться (механічно) | Референс |
|---|---|---|---|
| `pipeline-two-thirds-missing` — доба не має матеріального результату | С | Із 12 позицій закрито 6 (4 кроки + Clock/Report). Чотири законно `[ПОЗЖЕ]`. **Production=200 і Healing=900 не мають позначки і не підключені.** `BaseState.CurrentCycle` рахує власний час паралельно `DayProcessor.CurrentDay`. §4 прямо приписує «`AdvanceCycle()` превращается в шаг `Production`» — конверсію не зроблено, і жоден етап її за собою не закріпив | Starfield: аванпости як паралельна система без стику з основною |
| `no-cost-no-scarcity` — жодна дія міського лупа нічого не коштує | С | Grep `CostBundle\|ICostSink` по Assets і tools — жодного `.cs`, лише документація. При цьому §7 вписує ці порти у зміст **Э0**, а Э0 у CLAUDE.md значиться зробленим. Єдиний `TrySpend` у ядрі — прокорм у `BaseState`, який міський луп не викликає. Механічне тіло осі «час проти ризику» не має реалізації | Starfield: вкладення без ціни й окупності — добровільний тайм-сінк |
| `no-positive-ratchet-in-the-slice` — за 90 днів не росте нічого | С | `Tier { get; set; }` ніхто не змінює, крім інспектора демки; умови просування §3.6 не реалізовані; `PopulationState.Add` конвеєр не викликає жодного разу; ресурсів у лупі немає. Баланс усіх змін за прогін непозитивний. Це б'є в перший чек-пойнт: зріз 30 днів не може показати те, що має доводити | Frostpunk/CK: гравця тримає храповик — незворотне покращення |
| `crowd-band-invisible-and-unread` — втрата 30 людей невидима | С | `CrowdBand` не читає жодна формула; `BuildMoodboard(band, tier)` не має населення в сигнатурі, `Prosperity = tier - 1`. Після кризи місто виглядає і звучить ідентично. Це порушення §3.6 саме для ШКАЛИ (на відміну від мертвих полів) і інваріанта 4 для другої шкали | Frostpunk: втрата людей одразу видна на мапі й у Надії |
| `production-side-of-repo-does-not-exist` — у репо немає нічого, крім C# | С | `git ls-files ProjectSettings/` → лише `ProjectVersion.txt`. Нуль сцен, нуль `.meta`, нуль `uxml/uss`. У `manifest.json` немає localization/Input System/Addressables, хоча US-18.1 будує весь контент-пайплайн на Addressables-лейблах. Момент «відкрив Unity, натиснув Play» ще не ставався | — |

### Тема 7. Сигнали як шпалери

| Проблема | Сер. | Чому це станеться (механічно) | Референс |
|---|---|---|---|
| `post-report-carries-no-state` — доповідь не несе жодного байта стану | С | `PostReport` має п'ять полів і жодного під зміст. Ключ = `"post." + DomainTag + "." + Accuracy`, `Accuracy` — `Preview` статичного скіла проти статичного порога без штрафу за повтори. Ключ незмінний, доки гравець не переставив напарника. §5.8 обіцяє «сильний напарник дає КОНКРЕТИКУ» — конкретиці нема чим відрізнятися від розпливчастості | Football Manager: скаут при низькому знанні показує ДІАПАЗОН, що звужується. У нас ширина без оцінки |
| `content-budget-underestimated` — анти-повтору немає, а бюджет подвоєно | С | У `SignalComposer` немає `LastShownDay` і кулдауна на топік узагалі; `IRepeatTracker` живе тільки в перевірках. `MaxSignalsPerDay=4` застосовується всередині ОДНОГО `Advance`, а доба — два виклики → до 8 сигналів на добу проти §5.1.2. `Ambient` відсіюється в бурхливі дні й повторюється саме в тихі | CK3: кулдаун на конкретну подію — те, що знайшли моддери; Wildermyth: повтори на другій кампанії при пулі на порядок більшому |
| `band-change-topic-loses-direction` — падіння полоси читається як погіршення | П | `"tension.band." + change.To`, напрямок лише в тегах. Три з п'яти полос обслуговуються одним ключем в обидва боки. `SettlementChronicleDemo.cs:236` віддає «Меняется. И не в лучшую сторону» на будь-який `tension.band.*`. Тест перебирає лише зростання | Дельта-слот витрачено, а зміна прочитана навпаки |
| `quiet-day-has-no-delta` — гарантія, якої система не дає | П | Дельти народжуються лише з трьох джерел (предвісник, інцидент, зміна полоси); за їх відсутності `Select` виходить без заміни. §5.1 п.3 і назва тесту в §8 (`…AndAlwaysIncludeDelta`) обіцяють інше. Код чесний, док застарів | — |
| `localization-and-grammar-unplanned` — домен їде в репліку голим називним | С | §5.6 вимагає, щоб рівень 2 назвав домен ВСЕРЕДИНІ фрази, а `DomainTag` — рядок «улицы»/«рынок». Єдина реалізація ліпить його в дужках. Для слов'янської граматики потрібні словоформи, інакше пишеться вручну під кожну пару топік×домен — і множник, від якого рятував патерн «топики, не строки», зникає. `com.unity.localization` у manifest відсутній | — |
| `moodboard-art-combinatorics-never-costed` — сотні художніх станів ніхто не рахував | К | Prosperity 0..4 × Decay 0..4 = 25 незалежних станів (§5.7 вимагає незалежності), плюс довільні overlay-теги, 4 профілі Укладу, присутність 3-4 фракцій, день і ніч. У репо 0 сцен, 0 префабів, 0 текстур. Ані R10, ані N5 цього не рахують | Ігри зі станом міста тримають 3-4 рівні занепаду + проп-оверлеї саме тому, що матриця множить арт-борг |

### Тема 8. Наслідки і сторони

| Проблема | Сер. | Чому це станеться (механічно) | Референс |
|---|---|---|---|
| `repeat-penalty-punishes-the-world` — штраф за повтор карає за події, яких гравець не робив | С | `repeats?.Register(request.TopicId, day)` викликається безумовно і ДО перевірки `HasCandidate`; світові інциденти проходять цим шляхом. Поріг росте на +2 за спрацювання у вікні. Штраф невидимий і б'є, коли місто вже під тиском. Автор уже вимкнув повтори для доповідей із постів — тобто відчув проблему. §7 відносить цей штраф до Э2, а не до Э1 | Замкнена позитивна петля: Худша → +Напруження → вища полоса → більша Настійливість → частіші повтори |
| `incidents-are-only-a-tax` — інцидент віддає лише дельту прихованої шкали | П | `IncidentDefinition` не має поля винагороди; `IncidentResolver.Resolve` з усього `CheckOutcome` застосовує лише `TensionByBand`. Єдиний написаний канал винагороди — `PriceMultiplier` для підходу Торгівля (`market_brawl` оголошено саме з `ApproachForm.Trade`) — мовчки відкидається. Толків із §3.5 немає ані полів, ані споживача | Kingmaker, PoE: володіння як джерело виключно проблем → масовий перехід на Auto |
| `narrative-canon-empty-blocks-next-stage` — Э2 упреться в порожню теку | С | `ls docs/narrative/` → лише `MASTER_PROMPT.md`. Э2 на 60% контентний; виробництво канону — ручний процес в окремому чаті, який не стоїть у плані §7, не має обсягу й критерію готовності. Код Э2 буде написано під фракції, яких немає, і перевірено на заглушках | Це вже сталося з таблицею реплік у демках |

### Тема 9. Дуга кампанії й зовнішній луп

| Проблема | Сер. | Чому це станеться (механічно) | Референс |
|---|---|---|---|
| `first-hour-teaches-the-wrong-game` — онбординг продає не ту гру | К | §17.1: 5-20 хв — пролог-бій, 44-57 — виїзд із місією «бой + проверка», і лише 30-44 — перша петля бази. Поправка №3 переставила стовпи і перенесла чек-пойнт на «городской срез 30 дней», а §17.1 не переписали. Обидві аудиторії відсіюються: одна на 5-й хвилині, друга на третій годині | Frostpunk відкриває першим днем міста, а не сутичкою |
| `finale-tests-a-layer-the-game-never-taught` — фінал за шкалою, якої не існує | С | Grep «готовност» по docs/ і Assets/ дає ЄДИНЕ влучення, і те про критерій готовності етапу роботи. «Готовність» порушує власне §3.6: споживач є (складність фіналу), якоря і сигналу немає. Разом із першою годиною: тутор — бій, фінал — бій, гра посередині — місто | — |
| `stealth-does-not-exist` — обіцяний шлях не має носія | С | Поправка №1 першим пунктом ненасильницького шляху називає скрытность; складу скілів GDD (Эпік 2.2) її не містить, `SkillKeys` дзеркалить ті самі 10. **Уточнення:** `SkillKey` — struct над рядком, тож ядро й `CheckRequest` фізично можуть виразити стелс; блокує лише модель персонажа. Б'є не «у 90% проходжень», а у ще не спроєктованому шарі вилазок | Shadowrun: Dragonfall, Wasteland 3: стелсу як скіла немає — і тихого проходу як стилю там теж немає |
| `ui-surface-never-enumerated` — ніде не сказано, скільки екранів вимагає гра | С | З доків вичитується щонайменше 12 поверхонь плюс прохідне місто; жоден док не ділить їх на [ЯДРО]/[ПОЗЖЕ]. US-18.4 — одне речення. `git ls-files \| grep uxml` → 0. Для соло-новачка UI-поверхня — більший скоуп-ризик, ніж системи, а R1 рахує тільки системи | — |

### Тема 10. Інструменти, гігієна репо, доступність

| Проблема | Сер. | Чому це станеться (механічно) | Референс |
|---|---|---|---|
| `no-campaign-simulation-harness` — головна вигода детермінізму не використана | С | У `tools/` лише збірка й прогін тестів. Метрик ніхто не виводить, порогових перевірок на розподіли немає. Єдина заявлена метрика балансу — «4 з 5 тестерів називають полосу словами» (N1), тобто людський плейтест, якого в соло-розробника немає в потрібному обсязі. **Баланс цієї гри зараз не перевіряється ані машиною, ані людьми** | — |
| `balance-doc-guard-protects-the-dead-doc` — єдиний сторож прибитий до історичного доку | С | `BalanceDocTests` покриває `docs/BALANCE.md`, який CLAUDE.md оголосив історичним. §6 `SETTLEMENT_LAYER` не звірений нічим, і дрейф уже почався: у доці `SignalBalance` має `RumorBase/RumorPerMarginStep/RumorMaxExtra`, у коді — три інші поля | — |
| `readme-sells-the-cancelled-game` — головна сторінка описує скасований дизайн | П | README:3-6 обіцяє «Бой в духе XCOM» і «процедурные подземелья», а «Дальше по плану» відсилає до `docs/DESIGN.md`, який CLAUDE.md називає історичним. README і CLAUDE.md дають взаємовиключні відповіді на «що це за гра» | — |
| `accessibility-absent-and-designed-against` — дизайн активно проти доступності | П | Стан передається кольором сцени, щільністю NPC і відтінком фрази; числового дубля немає за принципом, журналу немає, субтитрів чи логу барків немає. Grep по docs/ на «доступност\|дальтон\|субтитр\|шрифт\|контраст» → 0. Метрика N1 вимірює проблему і нічого з нею не робить | Базові вимоги доступності — частина консольної сертифікації |
| `audio-channel-undesigned` — транспорт сигналу не спроєктовано | П | У жодному з чотирьох доків немає слів «звук/музика/озвучка/барк»; єдиний слід — коментар у `SignalDigest.cs:22`. Не ухвалено найдешевше рішення: барки текстом чи голосом — а воно визначає бюджет, локалізацію і те, чи можна почути предвісник, дивлячись в інший бік | — |

---

## Найнудніші місця (топ-10)

1. **300-400 натискань «далі» на кампанію проти 2-3 рішень на десяток діб.** Якір балансу — кампанія 150-200 діб (`TensionBalance.cs:9-12`), конвеєр крутиться по фазах. Гравець тисне `Advance` 300-400 разів; його ввід за весь цей час — розстановка на старті, галочка «патрулювати» і зрідка вибір у квесті. Демка чесно зізнається: `heavyChoiceDays = { 8, 18, 28, 38, 48, 58 }` — шість дат на 90 днів, вписані в інспекторі.

2. **Трек «площадь» доходить до 100%, лунає «Что-то готовят. Скоро», заряд списується — і не стається НІЧОГО. Кожного разу, всю кампанію.** Це не рідкісний баг, це єдина можлива поведінка кризи в поточному коді.

3. **Чотири незмінні ключі щодня в тихому місті.** `tension.ambient.<полоса>` (5 ключів на гру) плюс `post.<домен>.<точність>` (≈16 ключів на гру) складають рівно ті сигнали, що виживають бюджет у спокійні дні. Гравець читає той самий рядок близько 40 разів. Анти-повтору в композиторі немає взагалі.

4. **Одна й та сама доповідь з поста, доки ти не переставив людину.** Ключ = домен + точність, точність = статичний скіл проти статичного порога. Переставив напарника — ключ змінився один раз і завмер знову. За 150-200 діб це ~16 унікальних текстів на всю гру.

5. **Дев'ять інцидентів по 4-8 разів кожен, з тим самим `TopicId` і тим самим авторезолвом.** При порозі 100 і ставці 6-22 вулиця стріляє раз на 5-17 фаз; пам'яті теми немає, вигорання немає, кулдауна на конкретний інцидент немає. Це пропорція, що вбила радіантні квести Fallout 4.

6. **Розстановка розв'язується один раз на ~5-й день і не чіпається до кінця кампанії.** Протагоніст — кандидат на будь-яку позицію (`onPost || IsProtagonist`), у демці має 9 у всіх статах проти 7 і 6. Він виграє КОЖНУ перевірку на КОЖНІЙ позиції; 90 днів хроніки пройшли б так само, якби гравець не призначав нікого. Плюс у зрізі id позицій узагалі не збігаються, тож призначення там фізично ні на що не впливають.

7. **Патруль — галочка, поставлена раз і назавжди.** `patrolAtNight = true` в інспекторі й жодної зміни за 90 днів. Ціни немає (крок `Healing` не існує), нагорода є (всі нічні предвісники), придушення є (ставка ділиться на 3). Вибору, який Поправка 3.9 продає як щонічний, у лупі не існує.

8. **Тир 2 недосяжний очікуванням і недосяжний взагалі.** `TierPopulationRequirement = {0,400,900,1800}` при старті 200, а `PopulationState.Add` не викликається з конвеєра жодного разу — населення вміє тільки спадати від криз. Навіть у декларативному варіанті (приріст 1.5/добу) це ~133 доби чистого очікування до першої віхи.

9. **Тихий день не має дельти, і це закріплено тестом.** Дельти дає лише предвісник (тільки в момент підвищення ступеня), інцидент або зміна полоси. Більшість діб — жодної. Гравець читає чотири фонові репліки й не отримує відповіді на «що змінилося з учора».

10. **Друге проходження бітово повторює перше.** Сида немає, джерела фіксовані, селектор — арифметика від номера доби. `Pulse_SameInputs_IdenticalSchedule` закріплює це як досягнення. Питання «навіщо запускати вдруге» в жодному доку не поставлене.

---

## Що перевірили і що НЕ є проблемою

Ці претензії висувалися і **спростовані** — щоб не витрачати на них час повторно.

**Це задекларований дизайн, а не дефект:**
- *«У конвеєрі немає кроку введення — доба це кнопка»*: GDD.md:29 і US-1.3 прямо описують добу як кнопку очікування; агентність за дизайном живе ПОЗА конвеєром (призначення, ніч, квести, будівництво).
- *«Інцидент резолвиться сам, без модалки»*: US-8.2 [ЯДРО] прямо вимагає авторезолву релевантним напарником, а GDD:352-353 свідомо відмовляється від подієвої модалки («ігнор окремої події ні на що не впливає»).
- *«Порожня позиція = тиша, і тиша невідрізненна від спокою»*: Поправка 3.8 дослівно вимагає «никого на позиции — тишина» як подвійну ціну порожнього поста.
- *«Валентність вибору прихована — 4 з 5 виборів проходять безслідно»*: GDD:350 і :442 прямо забороняють показувати гранулярний ефект вибору; правило 3.4 — про полосу, а не про рух шкали.
- *«Сигнал не називає драйвер»*: те саме — атрибуція заборонена US-11.1; причинність подана предвісниками (домен + місце) і тегами інцидентів.
- *«Розстановка — бієкція архетип→слот»*: `PrimaryAptitude` не унікальний, 8 слотів проти 6 архетипів, є прямі конкуренції за одну людину; власна демка бази лишає слот порожнім.
- *«Перевірка бере max серед присутніх без ціни»*: max береться лише коли `RequiredPositionId` порожній; усі 9 інцидентів його мають.
- *«Штраф за повтори не має механічної різниці між підходами»* / *«persuade і trade — один стат»*: підходи різні механічно (Убеждение зсуває сходинку, Торговля дає `priceMultiplier`), а мапа статів — свідомо тимчасовий міст із коментарем «умирающий namespace».
- *«Детермінізм + вільні сейви = безкоштовний реролл»*: інвертовано — детермінізм прибирає найдешевшу форму savescum. (Але друга форма, розвідка майбутнього, лишається — див. `save-scum-turns-hidden-info-into-perfect-info`.)

**Це [ПОЗЖЕ]-фічі за планом §7, а не дірки:**
Будівлі як стан і дія (Э3); тир як зміна правил (Э2/Э3/Э4, механізм просування спроєктований у §3.6); фракції як сторони з конфліктом інтересів (Э2/Э4 — Указ, тертя між фракціями, Обов'язання з дедлайнами і `InsistenceBonusOnDefault` уже спроєктовані); Сплочённость і Знайомі (Э4); мудборд від населення й будівель (входи належать Э3/Э4); дренаж Храму й Укріплень (не має споживача, бо будівель ще нема — комбінація «вічний спокій» недосяжна); вилазки, стелс-верби, push-your-luck (Эпік 12, весь [ПОЗЖЕ]); Влияние без секції в балансі (споживач — рада, Э4); MVP на одній фракції (пряме рішення Поправки 3.1); прокорм по напарниках і лазарет (legacy Ітерації 1, який конвеєр дня не викликає); шість ресурсів і Materials з міського виробництва (той самий legacy, міграція вже запланована).

**Це помилки формулювання, не механіки:**
- Мертві поля `BloodyPath*` — це не порушення §3.6 (правило про ШКАЛИ), а просто мертві дані.
- `Position ids mismatch` — простори імен не різні: усі 7 id інцидентів є в `DefaultContent.AllSlots()`; розбіжність лише всередині демо-сцени.
- `MinDeltaSlots` не «перезаписує середину списку»: при `MinDeltaSlots=1` заміна завжди в хвості.
- `Тир 1 нерухомий` — фоновий тік не єдиний драйвер; п'ять крупних виборів = рівно поріг Ропоту.
- `Немає що втратити назавжди` — GDD перелічує: смерть, зраду в боса з усім гіром і рівнями, незворотні шрами, обрив арки; MVP-шлюз вимагає ≥1 зради і ≥1 смерті.

---

## Уроки з чужих ігор

Витяг найважливішого; повний масив розборів більший.

| Гра | Система | Вердикт | Механізм | Що беремо |
|---|---|---|---|---|
| Pathfinder: Kingmaker | Королівство: радники, BP, події з дедлайном | провал | Паралельна економіка з фейл-стейтом і ОДНИМ дієсловом («поклади радника і чекай»). Лікували обхідними шляхами: Auto-режим, потім складність Effortless | Наш день — рівно та сама форма. Якщо ти даєш «вимкнути систему», ти визнав, що вона не працює — а в нас вимкнути буде нікуди |
| Pathfinder: WotR | Хрестовий похід | провал | Друга повноцінна гра поверх першої; запобіжник (авто) карає — блокує квести й кінцівку | Систему, яку не можна вимкнути, не рятує жоден режим складності. Планка вища |
| Pillars of Eternity | Твердиня Caed Nua | провал | Пасивний генератор чисел + локація; напад без рішення оборони. Патч 3.0 переробив візитерів у дилеми з іменем і обличчям | Ліками був не новий стат, а рішення з ім'ям. У нас це відсутнє як категорія |
| Neverwinter Nights 2 | Crossroad Keep | спрацювало | Час іде від переходів гравця, а не від глобального годинника; немає домінантної стратегії; події від сюжетних умов, не від таймера | Найдешевша структурна правка нашого лупу: час рухається від дій, а не від кнопки |
| Neverwinter Nights 2 | Звіти Кани словами, без чисел | спірне | Із розпливчастих слів незрозуміло, це «добре» чи «посередньо» | Задокументований провал саме нашої ставки. Слова читаються як шкала лише коли їх мало, вони впорядковані й видно два сусідні значення |
| Dragon Age: Inquisition | Воєнний стіл | провал | Вибір радника — стат-чек із відомою відповіддю; таймер не є ціною; гру просять НЕ бути присутнім | «Обрав виконавця → просунув час → прочитав підсумок» — наша структура один в один |
| Frostpunk | Велика Буря | спрацювало | Ескалація невідворотна, відома за характером, невідома за величиною наслідку. Гравець не чекає — він готується | У нас телеграфія є, а невідворотності немає: криза вмикається лише коли гравець САМ нагнав полосу ≥3 |
| Frostpunk | Надія/Невдоволення словами | провал | Приховано не число, а ПРИЧИННЕ ПРИПИСУВАННЯ: бачиш поразку, не можеш відновити ланцюжок | Наш `tension.band.X` несе теги rising/falling і не несе `Driver`/`SourceId`. Та сама конфігурація |
| Frostpunk 2 | Фракції як постійний тиск | спрацювало | Фракція — це СТАН, а не подія: перечекати не можна, вона хоче чогось зараз | У нас фракція мовчить, доки я до неї не прийду; Обов'язання виникають лише як наслідок мого прохання |
| RimWorld | Raid points від багатства | спрацювало / спірне | Загроза — функція стану колонії, а не календаря. Ціна: відчуття покарання за ріст; розробник відповів перемикачем і публічною формулою | У нас тиск не залежить від досягнень узагалі. Розвинуте поселення тисне рівно стільки ж, скільки хутір |
| RimWorld | Adaptation factor | спрацювало | Після удару — перепочинок, після спокою — тиск. Темп як керована змінна | `Charge += Insistence(ctx)`, де ctx знає лише день/ніч/патруль/полосу. Режисури немає, кулдауни — стеля частоти |
| RimWorld | Mood відкритий, Storyteller прихований | спрацювало | Приховано планувальник, ВІДКРИТО стан із ітемізацією. Програєш несподівано, але ніколи незрозуміло | Ми ховаємо ОБИДВІ половини. Жодна розібрана гра так не робить і не вважається читабельною |
| Darkest Dungeon | Стрес + Афекти | спрацювало | Спершу хотіли лишити Стрес таємничим — після плейтестів відкрили шкалу і дельту числами, а приховали лише те, ЯКА поведінка вилізе | Наш розріз проведено рівно навпаки. Це головна неперевірена гіпотеза дизайну |
| Football Manager | Скаутинг: атрибути діапазоном | спрацювало | Невизначеність — це РЕСУРС, який купують зусиллям; діапазон завжди дає дві межі, тож рішення обґрунтоване навіть при нульовому знанні | У нас є ширина діапазону і немає оцінки: `PostReport` не несе фактів |
| Into the Breach | Повна інформація + детермінізм | спрацювало | Працює за трьох умов: стан влазить у голову, горизонт короткий, гравець ходить ПІСЛЯ того, як побачив загрозу. І навіть тут лишили один випадковий елемент | Ми взяли витрати детермінізму без його вигоди: інформація неповна, горизонт 200 діб, гравець не ходить після загрози |
| Factorio | Забруднення → еволюція → напади | спрацювало | Накопичувач причинний (гравець сам наповнює), ВИДИМИЙ як хмара, з джитером у моменті удару | Ми прибрали джитер саме там, де його лишила Factorio, і дали 4 репліки замість постійного просторового сигналу |
| Invisible, Inc. | Ескалація тривоги | спрацювало | Перша версія (20-30 рівнів) провалилася на плейтесті. Лікування — не випадковість, а ЧИТАБЕЛЬНІСТЬ: 6 рівнів × 5 порожніх підрівнів, наслідок тільки на переході, перейменування ALARM → SECURITY LEVEL | Найближчий реалізований прецедент нашої механіки — і він говорить протилежне §3.4 |
| Dishonored | Хаос — прихований лічильник насильства | провал | Прихована шкала від стилю гри перетворює механічний вибір на моральний вирок; ціну не видно в момент рішення. Arkane переробила в DH2 і прибрала в DotO | `PlaystyleBlood` — буквально Хаос. Поправка №1 обіцяє чесний обмін, а прихований штраф робить із нього мораль за спиною |
| Dwarf Fortress | Мовчазні поломки | провал | Система знає, що дварф не дійде до води, і мовчить. Гравець у стані «не знаю, чого я не знаю» | Наш аналог: незайнята позиція дає Худшу полосу І не подає жодного звуку |
| Dwarf Fortress | Dwarf Therapist / DFHack | провал | Попит на читабельність не зникає — він виноситься за межі гри | Дашборд нам заборонено технічно. Це знімає ризик витріщання на цифру, але не знімає попит |
| Cultist Simulator | Непрозорість + відсутність журналу | спірне | Непрозорість продає, доки стосується ЗМІСТУ, і вбиває, коли стосується ДОСТУПУ. Журнал минулого не руйнує таємницю майбутнього | Хроніка вже виданих топіків не порушує жодного нашого інваріанта |
| Against the Storm | Нетерплячість Королеви + ран-структура | спрацювало / спірне | Час має щось коштувати, інакше очікування — домінантна стратегія. І тиск персоніфіковано: той самий таймер, названий Королевою, читається як стосунки | У нас час коштує рівно нічого, а якір §3.1 документально гарантує, що чекати безпечно |
| Suikoden I/II | Замок від набору 108 Зірок | спрацювало | Зростання виражається новими ДІЄСЛОВАМИ й обличчями, а не числами; стрибки прив'язані до людей і сюжету | Наш аналог: між тирами лежать сотні днів плавного дрейфу без жодного нового дієслова |
| Banished / Manor Lords / Ostriv | Пізня гра, темп | провал | Без віх і без ціни за бездіяльність симуляція перестає ставити питання. Прискорення не лікує dead time — гравці на 3x усе одно грають у телефон | Наша дискретна модель просто перейменовує проблему: замість 3x гравець тисне «наступний день» — і ми навіть не зможемо це виміряти |
| Fire Emblem: Three Houses | Монастир vs Supports | провал / спрацювало | Незмінний ритуал = податок; викуповує його лише дискретна авторська сцена з жорсткими воротами | У Э0-Э3 немає ЖОДНОЇ разової авторської сцени. Ми взяли монастир без supports |
| Norland | Літнє оновлення 2025 | спрацювало | Лорд перетворений з ОБ'ЄКТА на АГЕНТА з власним гаманцем і власною метою: зник мікроменеджмент і з'явилася драма | Наші напарники — чисті об'єкти. Мінімум: напарник на посту раз на N днів САМ витрачає щось на свою ціль |
| Sid Meier / Raph Koster | Теорія вибору й нудьги | спрацювало | Вибір цікавий, коли жоден варіант не є явно кращим, варіанти не рівноцінні, і є інформація. Нудьга — брак нових ПАТЕРНІВ, а не подій | Наш день має рівно один патерн на 300+ повторів. Кожен тир мусить додавати новий ТИП рішення, а не новий інцидент |
| Game Developer, «Down Time vs. Dead Time» | Класифікація очікування | спрацювало | Питання-критерій: чи впливає те, про що я думаю зараз, на моє наступне рішення? | Готовий приймальний тест кожного дня нашого лупу |

---

## GURPS settlement management: що там насправді є

| Книга / стаття | Що моделює | Ключова процедура | Залежність від кубика | Що з цього портується | Статус перевірки |
|---|---|---|---|---|---|
| **GURPS City Stats** (2010) | Статблок МІСТА: середовище, люди, спроможності. Не цикл, а знімок | Заповнення бланка + 4 арифметичні формули (площа = населення/щільність; MR = дохід × населення × MBF; Hygiene = база TL ± обставини; DB = укріплення ± рельєф). Далі стати — модифікатори до 3d6 | відсутня при створенні, помірна при споживанні | Search Modifier → поріг «знайти майстра/товар/контакт»; Defense Bonus → поріг нічного патруля; Corruption → ефективний Уклад для «своїх»; Appearance → мудборд. Два трейд-офи книги переносяться безкоштовно: населення понад захищену місткість → або −DB, або −Hygiene; війна → падає дохід → падають MR | **Авторство суперечливе в наших джерелах:** один запис твердо каже William H. Stoddard і наголошує «НЕ Riggsby», інший — Matt Riggsby. Не покладатися на атрибуцію. Частина назв полів в одному записі прямо позначена як **непідтверджена** |
| **Pyramid #3/54, «City Management»** (2013, Matt Riggsby) | Те, чого немає в City Stats: **час**. Заснування, покращення, розширення міста | Місячний такт: кидок на обслуговування. Річний такт: оголосив проєкт → заплатив гроші → витратив рік → кинув 3d6 проти профільного навику (Finance/Administration/Architecture) → успіх зсуває стат на крок | критична | **Найкорисніший шаблон з усього корпусу.** Кидок → наша полоса за запасом, поріг показаний до підтвердження. Місячний maintenance → накопичувач зносу замість кидка. Триполюсний бюджет «варта / інфраструктура / розкоші» — це і є третя ставка, якої вимагає інваріант 2. Час стиснути: десятки днів, не 360 | Warehouse 23 у блербі приписує статтю Маршу — **схоже на помилку блерба**; зміст номера і кілька незалежних джерел дають Ріггсбі |
| **Low-Tech Companion 3** (2010-2011, Riggsby + Stoddard) | Матеріальна база доіндустріального поселення: скільки землі, праці й часу треба, щоб люди були ситі | Чиста бухгалтерія: населення × норма зерна ÷ врожайність = акри; акри × людино-дні = руки, поглинуті прокормом; різниця = надлишок | **відсутня** | Портувати не треба — воно вже детерміноване. Це джерело ЯКОРІВ під інваріант 6. Норми: активний дорослий ≈750 фунтів зерна/рік, селянин ≈600. Пшениця 355-660 фунтів/акр, картопля 2815-5280. Вибір культури = вибір ризику (монораціон карається) — ще один накопичувач із власною ставкою | Найнадійніший запис усього списку |
| **Basic Set** (Control Rating, Cost of Living, Wealth, Status, Rank) | Базовий словник, яким розмовляють усі інші книги | CR як фільтр законності й податків; Cost of Living як щомісячне автосписання під Status; Wealth масштабує дохід | відсутня | **Найцінніша ідея — Status ↔ Cost of Living: статус не нагорода, а ПІДПИСКА.** Готовий негативний зворотний зв'язок проти снігової кулі, якого в нас немає взагалі: чим вищий тир, тим дорожче просто утримувати | CR 0-6 і шкала Wealth підтверджені |
| **Boardroom and Curia** (2015, Riggsby) | Статблок ОРГАНІЗАЦІЇ: 18 статів у трьох групах (Capabilities / Costs and Values / Social Attributes) | Декларативний: заповнив блок → стати стають порогами для дій ПРОТИ організації або З нею. Реактивно | помірна / відсутня | Contacts-тір (Poor 12 / Basic 15 / Good 18 / Elite 21) як «яким скілом володіє організація» — якір для порогів; Resource Value (~0,5% від Startup Cost за місяць) як місячний бюджет послуг; Loyalty як модифікатор опору впливу. **Головний урок: місто і фракція мусять описуватися РІЗНИМИ наборами — середовище не має мотивів, агент має** | Повний перелік 18 статів **не верифіковано** — доступні витяги дають лише категорії. Полів з назвами «Reach» і «Secrecy» у блоці **немає** — наш `ReachCalculator` є нашим власним винаходом |
| **Social Engineering** (2011/2022, Stoddard) | Базова книга лінійки: реакція, вплив, торг, прохання, пошук | Реакція 3d6 + модифікатори; Influence rolls. **Expanded Influence Rolls (с. 31)** — те, що спільнота вважає головним виправленням | критична | Наш `CheckResolver` із полосами — це фактично Expanded Influence Rolls без кубика: **ми вже зробили те виправлення, яке вважають найціннішим у книзі**. Дозапозичити: правило «не сумувати два модифікатори за ту саму обставину» (= «один ефект — одна система», варте тесту) і complementary-кидок супутнього скіла (±2/±4) | Срібло ENnie 2012 |
| **SE: Pulling Rank** (2016, Sean Punch) | Rank як робочий ресурс: скільки й з якими наслідками можна випросити у СВОЄЇ організації | Assistance Roll: база від рангу + сходинка (**+1 за ранг вгору / −2 за ранг вниз — асиметрія навмисна**) + доречність (+5/−5) + complementary Administration + накопичувальний −1 за кожне повторне звернення. Провал на 10+ = дисциплінарна реакція | критична | **Ми вже переізобрели цю систему в Э2 — і наш варіант сильніший у трьох місцях:** немає кубика, `InsistenceBonusOnDefault` годує Пульс (чого в GURPS немає), провал видно наперед. Бракує: сходинки рангу, модифікатора доречності, complementary-кидка. Ймовірність замінюємо на ЗАПАС: «бракує двох пунктів», а не «шанс 62%» | Добре підтверджено |
| **SE: Back to School** (2015, Stoddard) | Навчання як соціальна структура: школи, наставники, гільдії | Ефективність = базова швидкість × модифікатори (кидок Teaching, підручники, обладнання, риси учня). Учнівство створює взаємні зобов'язання | помірна | Найлегший порт: наставницька позиція + рівень будівлі + вкладені руки = детермінований приріст. **Дає відсутній сьогодні ЛАНЦЮГ: населення → навчання → кваліфіковані працівники → вищі скіли на постах → кращі полоси.** Ненасильницький шлях зростання — прямо обслуговує Поправку №1 | Добре підтверджено |
| **Mass Combat** (2011) | Битви і — важливіше — УТРИМАННЯ сил | Maintenance ≈ **1/5 вартості підняття за місяць**; Logistic Strength — жорстка стеля сумарного утримання; логістика додає ~50% зверху | помірна (економіка — без кубика) | Бої нам не потрібні. Беремо три речі: коефіцієнт 1/5 як універсальне правило утримання будь-якої структури; **LS як СТЕЛЮ, а не витрату** (у нас немає стелі на кількість постів, і без неї гравець просто їх накопичує); суперлінійну ціну зростання. Плюс архітектурний урок: **не виносити кульмінацію в іншу підсистему з іншими правилами** | Добре підтверджено |
| **Realm Management** (2021, Christopher R. Rice) | Єдине офіційне доповнення саме про управління володінням | **Хід = місяць: (1) кидок випадкової події з таблиць лих/благ; (2) РІВНО ОДИН маневр** (Gather, Improve, Trade, Negotiate, Sabotage…) — кожен коштує Workforce Points і вимагає кидка проти вайлдкард-скіла Management. Дефіцит категорії RP = −1 за кожні 2 пункти мінусу до ВСІХ кидків, з самопоглибленням | критична | **Брати: структуру «подія → рівно один маневр»** (дефіцит уваги, що робить управління грою) і **механіку ДЕФІЦИТУ** (готовий четвертий накопичувач із від'ємним зворотним зв'язком). **Не брати:** таблицю випадкових лих (інваріант 1; наші накопичувачі + предвісники строго кращі), вайлдкард-скіл Management, чотири категорії RP без якорів (Agriculture у книзі нікуди не витрачається — це рівно порушення нашого §3.6), винесення війни в іншу книгу | Оглядачі рознесли книгу саме за стати без споживачів. **Наш інваріант 6 — буквально протиотрута; ціну доказу вже заплачено чужою книгою** |
| **Action 9: The City** (Sean Punch) | Перепрочитання тих самих статів під інший жанр | Нових статів немає: CR читається як «що поліція дозволить», Corruption — «кого купити», Population — «скільки жертв» | помірна | Портувати нічого — це урок дизайну. **Одна й та сама схована цифра має розповідатися мовою поточного заняття гравця.** Підтверджує наш шар сигналів: правильна відповідь на «як гравець дізнається про Hygiene −2» — не цифра, а різні репліки в різних контекстах | Добре підтверджено |
| Фанські порти правил королівства з Pathfinder у GURPS (2014+) | Не книга, а задокументована поведінка спільноти до 2021 | Три ортогональні шкали (Економіка / Лояльність / Стабільність), фази ходу, будівлі дають фіксовані бонуси | критична | **Механіку не брати — це еталон spreadsheet-бухгалтерії.** Але дві структурні речі: (1) трьох ортогональних шкал достатньо, більше зайве — підтверджує наш інваріант 2; (2) **будівля як НОСІЙ модифікатора: гравець ставить об'єкт, а шкала рухається як наслідок**. Три незалежні джерела сходяться на цьому — найсильніший сигнал усього дослідження | Тред forums.sjgames.com, вересень 2014 |

**Загальний висновок про GURPS:** система, яку хоче власник, у GURPS існує розкидано по чотирьох джерелах і **ніде не зібрана в один луп**. Статичний паспорт міста — City Stats. Час і проєкти — коротка стаття в Pyramid. Матеріальні якорі — Low-Tech Companion 3. Послуги й зобов'язання — Pulling Rank. Єдина спроба зібрати все (Realm Management) провалилася рівно за те, від чого нас захищає наше власне правило §3.6.

---

## Два напрями лупа навколо GURPS

### Напрям А — еволюція: «паспорт поселення + один маневр на добу»

Рушій не міняємо. Даємо конвеєру те, чого бракує: кілька прихованих статів (у кожного якір, споживач і сигнал) і **одну особисту дію на добу**. Дефіцитний ресурс — не золото (місто його не робить, і посуточну плату вже відкинула Поправка 3.10), а **вільні руки**: прокорм, проєкти й утримання постів тягнуть їх у три боки, і ця тяга дає третю й четверту ставку, яких бракує детермінізму. Проєкт матеріалізується спорудою на вулиці, а не «+1 до шкали».

| Шкала | Якір | Споживач | Сигнал | Видима |
|---|---|---|---|---|
| Вільні руки | 200 їдців тира 1 з'їдають ~90% рук; вільних ~20 = один проєкт АБО два пости. Фунти й акри — калібрувальні константи, гравцю не показуються ніколи | Production(200) прокорм; Construction(100) темп проєкту; Derived(400) утримання; Healing(900) руки в лазареті × Виучка медика | Три стани в доповідях («руки є / впритул / поля стоять»), недобудова не рухає візуальну стадію | ні |
| Стеля утримання | Тир 1 без складу тримає 2 пости; кожен рівень складу +2. Перевищення — штраф, **ніколи не заборона** | Derived(400): Σ утримання > стелі → −1 до точності всіх доповідей і +3 Настійливості треку «улицы» (модуляція наявного накопичувача, не новий драйвер) | Порожні полиці, буркотіння вартових, оверлей «палатки» | ні |
| Заслон | 0 відкритий хутір, 1 тин, 2 палісад, 4 стіна; пагорб +1, руїни −1 | Поріг нічного патруля = базовий − Заслон; Заслон ≥3 понижує укус кризи на сходинку | **Сам об'єкт стоїть на вулиці** — найчистіший випадок інваріанта 4: німого переходу фізично не буває | ні |
| Кривда | 0 вартовий не бере; 3 Уклад лише на папері. +1 за дві прострочені обіцянки або послугу в обхід; −1 за 20 діб без приводу | Ефективний Уклад = Уклад − Кривда для тих, у кого є довіра; ділить ефективність патруля; множник до `OrderTickMultiplier` | Власні канали, що НЕ перетинаються з Напруженням: вартовий дивиться в інший бік, заборонений товар на прилавку, ухильна доповідь | ні |
| Виучка домену | Стеля на два кроки нижча за напарника-спеціаліста; міський фахівець НІКОЛИ не дає Кращу полосу | `CheckResolver.Candidates`: фахівець стає кандидатом, якщо напарника немає — **це вперше робить «незайнята позиція = Худша» досяжним станом** | Доповідь перестає бути тишею і стає розпливчастою; підмайстер на вулиці | ні |
| Досяжність (наявна) | Без змін: 200 людей = 0 (§3.4) | **Другий читач:** поріг перевірок «знайти майстра / дістати товар / вийти на контакт» | Прилавок (уже спроєктовано) + хто взагалі доїжджає до міста | ні |
| Присутність (маневр доби) = 1 | Одна доба = один особистий маневр. Не накопичується, згорає при чеканні | Протагоніст — кандидат ЛИШЕ в домені, де він сьогодні присутній; відкриття проєкту й звернення до фракції витрачають маневр | **Єдина відкрита величина набору** — інакше рішення не існує | **так** |
| Ресурс фракції (Э2) | Скільки послуг фракція тягне за 30 діб: мала одну, велика три | `FavorDefinition.Cost` списується з бюджету. Вичерпаний бюджет = «зараз не можемо» — ІНША відмова, ніж низька довіра | «У нас самих зараз тонко»; їхні люди зникають з вулиць при незмінній довірі | ні |
| Запас прохання (Э2) | Показується як ЗАПАС у пунктах: «бракує двох — підніми довіру або зменш прохання» | `EffectiveScore` = довіра + сходинка (+1/−2) + доречність (±5) + супутній скіл − повтори, проти показаного порога | Поріг і запас до підтвердження (інваріант 8); відмова звучить причиною, не кубиком | **так** |

**Закриває:** `pipeline-two-thirds-missing`, `no-cost-no-scarcity`, `injury-has-no-consumer`, `protagonist-covers-every-post`, `waiting-is-free-dominant`, `patrol-is-free-dominant`, `one-active-track-most-of-the-time`, `three-tracks-one-variable`, `incidents-are-only-a-tax`, `post-report-carries-no-state`, `band-ceiling-flatlines`, `bloody-path-dead-data`, `crisis-kills-the-one-you-assigned`.

**НЕ закриває (їх треба полагодити ДО цього напряму, інакше він ляже на зламаний фундамент):** `crisis-gate-self-defeating`, `forewarning-lost-forever-when-asleep`, `forewarn-ladder-degenerates`, `tension-tick-doubled-by-phase`, `incident-selector-is-day-hash`, `forewarn-domain-mismatch`, `repeat-penalty-punishes-the-world`, `wait-does-not-interrupt`, `signal-budget-evicts-telegraphy`, `band-change-topic-loses-direction`, `quiet-day-has-no-delta`, `stealth-does-not-exist`.

**Головні нові ризики:** бухгалтерія (те, за що рознесли Realm Management — захист: жодного числа паспорта на екрані, тільки ступені; якщо тестер рахує руки на папері, напрям провалився); четверта прихована шкала поруч із Напруженням (потрібен тест «жоден TopicId не належить обом»); Виучка знецінює напарників (стеля −2 і заборона Кращої мають бути тестом, не коментарем); контент-вибух росте кратно при відсутньому анти-повторі; **усі якорі «на добу» підуть удвічі швидше, доки не полагоджено тік по фазах**.

### Напрям Б — радикальний: «поселення як персонаж»

Поселення отримує аркуш: чотири атрибути (Хребет, Уклад, Молва, Норов), скіли по доменах, трейти й шрами з біографії, два пули — Запас (матеріальне) і Нерв (перейменоване Напруження з тим самим internal-значенням і полосами). Головна нова механіка — **Руки: 3-6 очок дії щоранку**, витрачаються на маневри (Праця, Будівництво, Навчання, Слово, Розвідка, Похід, Особисто). Скіл поселення більше не «максимум серед присутніх»: він складається з рівня будівлі + держателя поста + навченої частки мешканців, а протагоніст входить у перевірку лише як окремий дорогий маневр. Тиск тече не з календаря, а з дефіциту.

| Шкала | Якір | Споживач | Сигнал | Видима |
|---|---|---|---|---|
| Хребет 0..20 | 1 очко = 25 дорослих (одна робоча ватага). Хутір-старт = 8 = 200 людей = Досяжність 0 — той самий якір §3.4 | Руки = floor(Хребет/2) − вибулі; Стеля Запасу; поріг просування тиру | Скільки людей видно на полях; вільні Руки вранці; стадія комори | ні |
| **Руки 0..8** | 1 Рука = денна праця однієї ватаги. Хутір 3, містечко 6 | Кожен маневр списує Руки; невитрачені йдуть у Працю з гіршою полосою; Похід і Особисто по 2 | Сам лічильник + хто фізично зайнятий | **так** |
| Запас | 1 Запас = доба харчування одного очка Хребта. «Запасу 40 при Хребті 8» = «зерна на п'ять днів» | Obligations(600) щодня списує утримання; `CostBundle` маневрів; від'ємний Запас → Дефіцит | Прилавок і полиці, стадія комори, дитячі порції, «вистачить до такого-то дня» | **так** |
| Нерв (= Напруження) | 1 очко = одна несправедливість, стерплена без слова. Пороги 200/400/600/800 без змін | Поріг стримування інциденту; вага таблиці; на дні полоси — криза. **Драйвери НЕ розширюються** | Уся наявна таблиця §5.2 без жодної зміни — найдорожча вже написана частина Э0 | ні |
| Скіл поселення в домені | Поріг = скільки дає середняк на поточній стадії (наш чинний якір §3.3). Скіл = 2×рівень будівлі + держатель поста + навчена частка − Дефіцит | `CheckResolver` порівнює саме це число. **Порожній пост відсікає найбільший доданок, а не дає магічну Худшу полосу** | Точність доповіді і є показником скіла; видимий поріг проти видимого скіла до підтвердження | **так** |
| Уклад 0..4 | 0 ніхто нічого не питає; 4 комендантська година. Наші чотири стани лягають без змін | Множник фонового тіку (є), профіль прилавка й ночі (є), **плюс щоденна плата Нервом за кожен ступінь** — тримати лад дорого | Таблиця §5.5 повністю + тон патруля | **так** |
| Молва 0..4 | 0 «десь там є хутір»; 4 купці роблять гак, щоб зайти | Зсув порога пошуку; приплив новоприбулих; `ResponseDelayDays`; Настійливість накопичувача «Чутка» — успіх сам породжує увагу | Хто приходить до воріт цього тижня; різноманіття прилавка; чи впізнають тебе на дорозі | ні |
| Норов 0..6 | Скільки діб нестатків громада витримує. Норов 3 ≈ тиждень | Відновлення Нерва за ніч; поріг конвертації невиконаного Зобов'язання в інцидент; опір перекупу поста | Чи йдуть добровольці в патруль без наказу; тон у черзі до комори | ні |
| Трейти й Шрами поселення | Трейт лише за пережиту подію («Обпечене» після пожежі). Слотів фіксовано; шрам вічний і слота не займає | Флет-модифікатор до скілів доменів; відкриття/закриття маневрів («Вдовин ряд» закриває Похід повним складом) | Хроніка міста + фізичний слід у поселенні + містяни, що згадують через десятки днів | **так** |
| Дефіцит 0..6 | 1 очко за кожні дві доби, на які Запасу не вистачило | Плоский штраф до порога ВСІХ перевірок; множник Настійливості накопичувача «Виснаження» | Доповіді гіршають одночасно у всіх доменах — найпомітніший можливий сигнал | ні |

**Закриває** усе з напряму А **плюс** `passive-drift-doom-clock-at-high-tiers`, `incident-selector-is-day-hash`, `stealth-does-not-exist`, `tension-tick-doubled-by-phase`, `wait-does-not-interrupt`.

**Ціна:** вимагає викинути Эпік 3 (тактичний бій цілком), Эпік 12 (процедурні данжі), Эпік 11.2 («Готовність»), Эпік 8.2 (Совет як окрема система), скасувати пункт Поправки 3.10 про посуточну плату, зламати якір §3.1 і тест `Tension_PassiveCampaign_NeverReachesFracture`, переанкорувати `DayReport`, `IncidentResolver` і частину з 93 тестів. Найбільший ризик — це рівно та мінігра-spreadsheet, за яку рознесли Realm Management і Pathfinder-королівства; він первинний, а не побічний, і валідується тільки зрізом 30 днів.

---

## Що це ламає в GDD v6

**Уже зламане, незалежно від напряму (виправити в будь-якому разі):**
- **US-8.2 містить два взаємовиключні правила в одному реченні** (GDD.md:273): «протагонист — тоже кандидат» І «если позицию никто не держит → худший исход». За присутнього протагоніста друге математично недосяжне — разом із «сліпотою» Поправки 3.8, яка за пріоритетом вища за GDD. Код чесно відтворив суперечність доку.
- **§17.1 «Первый час» не переписаний** після Поправки №3, яка переставила стовпи і перенесла чек-пойнт на «городской срез 30 дней».
- **US-1.3 проти US-1.5**: «явне ждать N дней» проти «отдельной кнопки ждать нет». Досі нешкідливо; з будь-яким маневром доби стає критичним.
- **§5.1 п.3 і рядок таблиці §8** обіцяють дельту щодня, код цього не робить і не фабрикує — треба вирішити, що саме гарантовано.
- **§4 приписує «`BaseState.AdvanceCycle()` превращается в шаг Production»** — конверсію не зроблено, жоден етап її не закріпив, `DayReport` не має поля `Production`.
- **README** описує скасований дизайн і веде в історичні доки.
- **§7 вписує порти `CostBundle` у зміст Э0**, а Э0 значиться зробленим — при нульовій реалізації.
- **Поправка №3.11**: усі згадки конкретних настільних систем-джерел (City Stats, Defense Bonus, Control Rating, Contacts, Resource Value, Logistic Strength, Assistance Roll) мусять бути перейменовані в питомі терміни **до першого коміту**, і жодна таблиця не переноситься набором діапазонів.

**Ламає напрям А (потрібні рішення власника):**
- **Поправка 3.10 прямо відкидає «Болезни и гигиена»** — тому весь очевидний порт Hygiene → Healing(900) **заборонений**; у напрямі А Healing отримує споживача через вільні руки, без шкали чистоти.
- **Поправка 3.10 відкидає «посуточную плату за статус»** — утримання постів у руках зі стелею формально не те, по суті сусіднє. Потрібен дописаний абзац: «утримання дозволене у ресурсі, який місто саме виробляє, і тільки як стеля зі штрафом, ніколи як дренаж до нуля».
- **Поправка 3.10 фіксує рівно чотири поля фракції**; Ресурс фракції і Contacts-тір — п'яте й шосте.
- **Найбільший конфлікт — матеріал для проєктів.** Эпік 6.2 і 15: міського виробництва матеріалів немає, обидва компоненти тільки з вилазок; US-6.4: «третьего ресурса нет». Якщо проєкт коштує лише рук і днів — вилазки знецінюються (порушення Эпіка 15). Якщо компонента — темп головного лупу впирається у шар, якого в коді немає взагалі. Третій, суто міський матеріал розв'язує це і суперечить обом. **Рішення потрібне до першого рядка коду.**
- **US-7.5** («абстрактное растущее население, чтобы не вести поштучный учёт рабочих») доведеться переписати: «не ведём поштучный учёт» лишається обіцянкою UI, не обіцянкою моделі.
- **Эпік 7 + інваріант 3**: архітектурний тест сьогодні охороняє тільки Напруження; `Game.Gameplay` зможе прочитати Заслон чи Кривду без помилки компіляції. Тест треба розширити.
- **§3.6**: `TierPopulationRequirement = {0,400,900,1800}` доведеться перерахувати від несної здатності, і §3.6 переписується цілком.

**Ламає напрям Б додатково:** Эпік 3 і Эпік 12 повністю; Эпік 11.2; Эпік 8.2; скорочення Эпіка 5, 6.1 і 1; скасування якоря §3.1 і його тесту; переписування §4/§7 (Construction/Production/Population/Derived/Healing перестають бути `[ПОЗЖЕ]`); переписування Прил. Б (уся бойова корзина чисел) і Прил. В (R6, R7, R9, R11, R13 і сам шлюз валідації, який зараз — вертикальний зріз бою).

**Лишається забороненим у ОБОХ напрямах:** таблиця випадкових лих/благ на початку ходу (інваріант 1 — наші накопичувачі + предвісники строго кращі); вайлдкард-скіл «Management» (перевірки навмисно прив'язані до конкретних `SkillKey` і конкретних людей); винесення кульмінації в іншу підсистему з іншими правилами — криза мусить розв'язуватись тим самим `CheckResolver`, лише з іншими порогами.

---

## Рекомендація і перші три кроки

**Рекомендація: спершу «Етап −1» (ремонт фундаменту), потім напрям А, і тільки після зрізу 30 днів — рішення про Б.**

Обидва напрями пропонують нові шкали й нові споживачі. Обидва лягають на луп, у якому криза не може спрацювати, предвісник не доходить, лічильник часу бреше вдвічі і стан неможливо зберегти. **Будувати паспорт поселення на цьому фундаменті означає загорнути зламану механіку в дорожчу обгортку.** Ремонт коштує днів, а не тижнів — це п'ять точкових правок у ядрі й кілька перепрописаних тестів.

Напрям А кращий за Б як **база** з трьох причин: (1) він не вимагає скасування Эпіка 3 і 12 і не переписує 93 тести; (2) він наповнює вже оголошені порожні кроки конвеєра, тобто це еволюція, а не переписування; (3) його головна ідея — один маневр на добу — і є те, чого бракує за критерієм Мейєра, і при цьому вона сумісна з Б. Напрям Б варто тримати як **горизонт**: якщо зріз 30 днів покаже, що одного маневру мало і місто читається як таблиця, аркуш поселення — правильний наступний крок. Але Б скасовує половину GDD, і таке рішення не ухвалюють до плейтесту.

### Крок 1. Ремонт телеграфії і часу (без нових механік)

1. **Криза.** Не обнуляти `AnnouncedLevel` у `Fire()` — зберігати `AnnouncedLevelAtFire` у `PulseTick`/`DayContext`, або перенести умову «оголошено 3-ю ступінь» усередину `IsReady`, щоб накопичувач не спалював заряд даремно. Розділити `ReachedLevel` (внутрішній) і `DeliveredLevel` (що дійшло до гравця) і повісити ворота кризи на другий. Підключити `CrisisGraceDays`, який оголошено і не читається.
2. **Час.** Розвести `Day` (календарна доба) і `Phase` у `DayContext`: добу інкрементувати лише на переході Ніч→День, тік тира давати лише вдень, усі вікна в днях рахувати від `CalendarDay`. Прибрати `report.Day / 2 + 1` з демки.
3. **Драбина предвісників.** Після `Fire` скидати `Charge` у 0 або гасити `ForewarnLevel` на K фаз; заборонити `Fire` у фазу, в яку вперше оголошено ступінь 3.
4. **Домен.** Передати `source.DomainTag` у `IncidentTable.Pick` як фільтр.
5. **Тести замість вакууму.** `Assert.Pass("Кризис не наступил")` → `Assert.Fail`, якщо кризи не було за N діб на Изломі. `Tension_PassiveCampaign_*` → на `AdvanceFullDay()` і на всі чотири тири. Новий тест `Crisis_Level3_PrecedesFireByAtLeastOneDay` і `Forewarn_Domain_MatchesFiredIncidentDomain`.

### Крок 2. Симуляційний харнес і контракт сейву — до будь-якого балансу

- `tools/Alpha.Sim`: CLI, що жене кампанії за набором політик (пасивна / патруль щоночі / агресивні квест-вибори) на всіх тирах і пише CSV: коли перша криза, скільки діб без дельти, скільки сигналів на добу, як часто пост порожній, скільки напарників гине, скільки разів повторився кожен `TopicId`. Поверх — межові тести на **розподіли**, а не на одну точку. **Це головна вигода нашого детермінізму, і вона зараз не використана взагалі.** Без неї баланс цієї гри не перевіряється ані машиною, ані людьми.
- DTO-знімки в `Game.Core` (`TensionSnapshot` / `PulseSnapshot` / `DaySnapshot`) з приймаючими конструкторами і round-trip тест: «зберегли на 50-й добі, завантажили, прокрутили 50 діб — логи збігаються з безперервним прогоном». Заразом — рішення власника про сейв на межі доби (інакше сейв-скам знімає всю приховану інформацію назавжди).
- Скопіювати патерн `BalanceDocTests` з мертвого `BALANCE.md` на живий `SETTLEMENT_LAYER §6`: по тесту на секцію, плюс тест «кожне поле, назване в §6, існує в класі».

### Крок 3. Одне рішення на добу — мінімальна версія напряму А

Не весь паспорт одразу. Мінімум, який робить добу грою і який можна перевірити зрізом на 30 днів:

- **Присутність = 1 маневр на добу.** Протагоніст — кандидат ЛИШЕ там, де він сьогодні стоїть (`|| a.IsProtagonist` замінюється на явний маневр). Це один рядок у `CheckResolver`, і він одночасно вмикає три мертві механіки: «незайнята позиція → Худша полоса», `PostReport.Silent` і «сліпоту» Поправки 3.8.
- **Інцидент стає запитом.** Крок `Incidents` кладе в `DayContext` `PendingDecision` (інцидент + прев'ю полос для кожного доступного підходу/шляху + ціна в днях), `Advance` повертає звіт зі статусом «очікує рішення», резолв — наступним викликом. Це архітектурна зміна `DayProcessor`, і закладати її треба **зараз**, поки на `Advance()` не зав'язали Э2-Э4. Тут же оживає `BloodyPath*` і вперше показується поріг.
- **`ProductionStep(200)`** — обгортка навколо вже написаної `AdvanceCycle` (у `Game.Core.Base`, щоб не впав `ArchitectureGuardTests`), з прибраним власним лічильником циклів. Найдешевший спосіб дати добі матеріальний результат.
- **Каталог позицій — один.** Контент-тест «кожен `RelevantPositionId` і `PostDomain.PositionId` існує в каталозі слотів»; демку і смоук-тест перевести на `DefaultContent`-слоти, інакше зріз 30 днів знову нічого не доведе.
- **Перед першою сотнею реплік** — рішення про базову мову, словоформи домену і барки текстом чи голосом; і `LastShownDay` у композиторі, бо без анти-повтору кожна нова шкала множить шум, а не читабельність.

**Приймальний критерій зрізу 30 днів** (з Поправки 3.1, доповнений тестом «Down Time vs Dead Time»): візьми будь-яку добу і спитай — (а) який вибір гравець робить саме сьогодні, (б) чи має він інформацію для нього, (в) чи є другий варіант, який теж має сенс, (г) чи змінить сьогоднішній сигнал завтрашнє рішення. Якщо на будь-яке «ні» — цю добу можна викинути з гри без втрат.