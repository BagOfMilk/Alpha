# Поправки до GDD v6

> Джерело істини проєкту = **GDD v6 (`docs/GDD.md`) + поправки в цьому файлі**.
> При конфлікті поправка перекриває текст GDD.
>
> **Нумерація — за датою рішення власника** (рішення власника, 20.09.2026:
> «Б — голод №4 по дате»). Поправки писалися у двох гілках паралельно, і номер
> №4 виявився зайнятий двічі: «Прокорм поселення» від 29.08 тут і «Люди з
> іменами» від 05.09 у гілці `claude/main-gameplay-loop-0af80b`. Старша за датою
> лишається №4, друга при злитті стає **№5**.
>
> **Що зробити при злитті цих гілок:** у матеріалах «Людей с именами»
> замінити `Поправка №4` і підпункти `№4.0–№4.9` на `№5` і `№5.0–№5.9` —
> це 36 згадувань у шести файлах (`GDD_AMENDMENTS.md`, `FIRST_HOUR.md`,
> `CLAUDE.md`, `QUEST_CANDIDATES.md`, `DefaultContent.cs`,
> `PresenceAndCatalogTests.cs`).

---

## Поправка №1 — Стовп «Ненасильство понад усе» (2026-08-12)

Рішення власника проєкту.

**Правило.** Гра будується максимально ненасильницьки: у будь-якого завдання
(квест, місія, кімната данжу, міська криза) є щонайменше один
**ненасильницький шлях** — скритність, Переконання/Залякування/Торгівля,
Злом/Механіка, обхідний маршрут. **Насильницький шлях існує
завжди** і вирішує завдання швидко, але він ДУЖЕ складний.

**Баланс-вісь «час проти ризику»:**
- **Тихий шлях:** безпечний (без ран/шрамів/пермадета), але повільний і
  дорогий — витрачає дні, ресурси, підготовку, іноді зобов'язання перед
  фракціями. Доступність шляху вирішується підготовкою (детерміновані
  перевірки скілів, US-2.6), а не костями.
- **Кривавий шлях:** швидкий (менше днів і кроків), але дуже важкий бій —
  смертельно небезпечні вороги, рани в днях лікування, вічні шрами, ризик
  незворотної смерті. Насильство = купити швидкість ціною ризику.
- Важелі в `BalanceConfig`: множник складності боїв, ціна тихого шляху в
  днях/ресурсах, пороги перевірок.

**Що уточнюється в GDD v6:**
1. **US-10.2** (соц-скіли гейтять обхід бою): з бонусу стає **системним
   правилом для всього контенту**: обхід є завжди (не тільки через соц —
   також скритність/злом/маршрут).
2. **Епік 6.2 / 15** («не-боевого пути нет»): зв'язка squad-loop → settlement-loop
   зберігається — за матеріалами як і раніше треба виходити назовні, але вилазка
   не зобов'язана закінчуватися боєм: стелс-прохід і переговори теж здобувають.
   Формула: «не-бойового шляху немає» → «не-вихідного шляху немає».
3. **Епік 3** (бій): числа і механіки GDD зберігаються, але тюнінг — під
   «дуже складно»: щільність загроз і ціна помилки вища. Бій — усвідомлена
   ставка, а не дефолт.
4. **Епік 13** (місії): посилення US-13.2 — у летальних вузлів завжди є
   телеграфований тихий обхід.
5. **Наратив:** у напарників явне ставлення до насильства (через цінності,
   US-9.2); світ, фракції і Напруга реагують на стиль проходження
   (різанина ростить Напругу і лякає NPC; милосердя запам'ятовується).

**Перевірка в плейтесті (додається до шлюзу валідації Додатка В):**
вертикальний зріз повинен показати, що (а) тихий шлях — не «нудний дефолт»,
а осмислена гра в підготовку, і (б) бій — не єдиний спосіб встигнути,
але реально швидший і реально страшніший.

---

## Поправка №2 — «Персонажі з суспільного надбання» (2026-08-12)

Рішення власника проєкту.

**Правило.** Усі іменні персонажі гри — напарники, NPC, лідери фракцій,
боси, згадувані легенди світу — це **переосмислені персонажі із
суспільного надбання (public domain)**: міфологія, фольклор, легенди,
класична література. Виняток — протагоніст: він кастомний і
створюється гравцем.

**Межі безпеки:**
- Лише першоджерела, безумовно наявні в суспільному надбанні
  по всьому світу (орієнтир: автор помер понад 70 років тому; публікації до
  ~1930 р.) і фольклор/міфологія.
- Заборонені елементи з пізніших захищених версій (кіно, комікси, Дісней
  тощо) — беремо лише риси вихідного тексту/міфу.
- Обережно з товарними знаками: імена на кшталт Tarzan, Zorro, Conan можуть
  бути trademark — уникати або брати форму імені з оригіналу; за
  сумніву обирати іншого персонажа.
- У картці кожного персонажа фіксується поле **«Першоджерело»**
  (твір/міф, автор, рік, підстава PD-статусу) — це частина канону,
  експортується в `docs/narrative/companions.md` тощо.
- Переосмислення під сеттинг (постапок, техно/мутації): зберігаються ім'я
  і впізнаване ядро характеру/легенди, обставини адаптуються.

**Навіщо:** юридична безпека + безкоштовна впізнаваність + готові
архетипи й очікування, які можна обігравати й ламати (римується з
«желание/рана/ложь» і чеховськими рушницями зрад).

**Вплив на системи:** лише наратив (кастинг, діалоги, бентер) і
маркетинг; механік не стосується.

---

## Поправка №3 — «Живий шар поселення» (2026-08-12)

Рішення власника проєкту. Найбільша з поправок: змінює вагу стовпів.

### 3.1 Перевага стовпів: міський цикл — головний луп

**Правило.** Основний зміст гри — життя поселення день за днем:
призначення, події, фракції, зростання від хутора до містечка. **Бої дуже
рідкісні.**

Вилазки зберігаються (Поправка №1: «не-вихідного шляху немає» — обидва компоненти
приходять лише ззовні), але **всередині вилазки бій — рідкісне, дороге, усвідомлене
рішення**, а не зміст. Норма — тихий прохід: скритність, переговори, злом,
обхідний маршрут.

**Що змінюється в GDD v6:**
1. **Епік 0, порядок стовпів.** Було: 1) сюжет, 2) база, 3) бій, 4) данжі/лут.
   Стало: **1) сюжет, 2) місто і його життя, 3) вилазки (переважно тихі),
   4) бій як рідкісна подія.** Механіки Епіка 3 зберігаються повністю — змінюється
   їхня частота, а не устрій.
2. **Додаток В, шлюз валідації.** Перший чекпойнт розробки змінюється з
   «вертикальний зріз бою (сутичка 4 проти ~6)» на **«вертикальний зріз
   міського циклу (30 днів) + плейтест першої години»**. Бойовий зріз лишається
   обов'язковим, але йде на другий пріоритет: він валідує рідкісну подію,
   а не основний цикл.
3. **Додаток А, MVP-зріз.** Міський шар перестає бути «на минималках»:
   у MVP входить повноцінний денний цикл, інциденти з телеграфією і одна
   фракція зі ступенями довіри. Данжі лишаються заглушкою-джерелом матеріалів.

### 3.2 Дуга кампанії: хутір → містечко

Тіри міста (US-7.6) перестають бути фоновим множником загроз і стають
**хребтом кампанії**. Кожен тір змінює не числа, а правила гри:

| Тір | Назва | Що з'являється | Чим відчувається |
|-----|----------|----------------|---------------|
| 1 | Хутір | позиції, денний цикл, чутки | усіх знаєш на ім'я; подія раз на кілька днів |
| 2 | Село | перша фракція, ступені довіри, ніч як вікно загроз | з'являються чужі; вночі страшно |
| 3 | Слобода | друга фракція і тертя між ними, Уклад, Досяжність | політика; прилавок росте; треба обирати сторону |
| 4 | Містечко | патрони, рада, розкол фракцій, кризи | місто живе без тебе; ти його ведеш, а не контролюєш |

### 3.3 Повний детермінізм

**У ядрі немає `System.Random`.** Планувальник подій — детермінований
накопичувач: у кожного джерела тиску своя **Наполегливість** (очок на добу),
подія спрацьовує при переповненні **Заряду** над порогом. Жодного джитера,
жодних прихованих кидків.

**Непередбачуваність береться з неповноти інформації, а не з костей:**
- багато паралельних накопичувачів з різними ставками — передбачуваний кожен,
  але не їхня сума;
- ставки змінюються від дій гравця, тому розрахунковий строк «пливе»
  після кожного ходу;
- пороги і заряди приховані (числа гравцю не показуються);
- гравець не знає навіть повного списку активних джерел, поки передвісник
  не назве домен.

> Шахи повністю детерміновані і при цьому непередбачувані.
> **Плата за це рішення:** дизайнер зобов'язаний тримати не менш ніж три активні
> накопичувачі з різними ставками, інакше система читається наскрізь.
> Це інваріант, а не рекомендація.

### 3.4 Шар сигналів — частина системи, а не UI

Будь-яке знання гравця про приховані шкали проходить через один шар сигналів.
**Правило «немає німого переходу полоси»:** зміна полоси будь-якої прихованої шкали
зобов'язана породити спостережуваний сигнал (репліка, зміна вулиць, мудборд).

Технічно: числові поля денного звіту — `internal`. Збірка `Game.Gameplay`
фізично не може прочитати Напругу, тому дашборд неможливо зібрати
навіть помилково. Це технічне втілення US-17.2 і US-7.1.

### 3.5 Драйвери Напруги закриті

Нові системи **не додають драйверів** — вони лише модулюють наявні
(наприклад, Уклад — множник до фонового тику від тіру, а не нове джерело).
Білий список драйверів — дані в `BalanceConfig`; спроба застосувати драйвер
поза списком відхиляється і покрита тестом.

### 3.6 Правило нової шкали

Шкала не додається в гру, поки в неї немає трьох речей:
1. **Якір** — що означає конкретне значення («200 человек = стартовое село =
   Досягаемость 0»);
2. **Споживач** — хоча б одна формула, яка її читає;
3. **Сигнал** — як гравець побачить її стан без числа.

Це ліки від задокументованого провалу подібних настільних систем:
абстрактні ресурси без опорної точки, стати, які не читає жодна формула,
і відсутність відповіді на питання «как у меня дела?».

### 3.7 Уточнення US-2.6 — полоси наслідку

Детермінована перевірка дає не «успех/провал», а **полосу наслідку** за
запасом над порогом: Найгірша / Базова / Хороша / Найкраща. Поріг як і раніше
показаний заздалегідь, костей як і раніше немає.

**Підхід задає форму драбини:**
- **Переконання** — піднімає підлогу (у межах подушки не дає Найгіршу) і опускає
  стелю (Найкраща недоступна): страховка від хвостів;
- **Залякування** — подовжує обидва хвости: Найгірша додає штраф до відношення,
  Найкраща досяжна з меншим запасом;
- **Торгівля** — полосу не змінює, конвертує запас у ціну.

Незайнята релевантна позиція (US-8.2) — окремий випадок того самого механізму:
кандидатів немає → Найгірша полоса.

### 3.8 Уточнення US-8.1 — доповідь з постів

Напарник на позиції дає не лише сплеск ефекту будівлі й резолв подій, а й
**доповідь** — раз на добу усне суб'єктивне зведення по своєму домену (варта —
про вулиці, ринок — про ціни і приїжджих, лазарет — про людей, таверна — про чутки).
**Точність доповіді йде за тією самою драбиною полос:** сильний профільний напарник
дає конкретику, слабкий — розпливчастість, нікого на позиції — тиша.

Це діегетична відповідь на «как у меня дела?» без панелі і нова ціна пустої
позиції: не лише гірший наслідок події, а й **сліпота**.

### 3.9 Ніч

Уточнення US-1.5: **спати = втрачати сигнали; патрулювати = отримувати нічні
передвісники** ціною відпочинку і лікування. Це дає патрулю небойову нагороду —
інформацію — і пов'язує ніч із шаром телеграфії.

### 3.10 Що відхилено і чому

- **Подобова плата за статус** — місто майже не виробляє золота
  (міського виробництва матеріалів немає), постійний відтік створює ризик
  софт-локу і вимагає показати баланс числом. Корисне зерно («статус
  витрачаємо, а не куплений назавжди») забрано через Зобов'язання.
- **Хвороби і гігієна** — будують саме ту спіраль «лікуюся → криза б'є
  ослабленого», яку забороняє US-1.3; плюс шкала, яку читала б
  лише вона сама. Цікавий наслідок (відтік населення) вже є як
  криза-наслідок US-11.1.
- **Деградація від перекачування ресурсів** — з міста нема чого викачувати.
  Зерно («повторна експлуатація дорожчає») забрано як штраф за повторні
  звернення до однієї фракції.
- **Утримання армії і спіраль втрат** — конфлікт із «софт-лок невозможен».
- **Повний стат-блок організації** — дванадцять полів, споживач є у
  чотирьох. Залишені: відношення, згуртованість, чуйність, внесок у
  досяжність.

### 3.11 Походження і юридична рамка

Шар спроєктований за мотивами **настільних систем управління володінням**
(домен, поселення, організація). Запозичені лише **механічні патерни** —
ідеї, процедури і системи, які авторським правом не охороняються.

**Обов'язкові правила:**
- усі терміни — свої; усі числа перераховані на власній кривій;
- жодна таблиця не переноситься «як є» (набір діапазонів + ярлики +
  порядок рядків — це охоронюваний відбір і розташування);
- **назви конкретних настільних систем-джерел не згадуються** ні в
  репозиторії, ні в документації, ні в коді, ні в титрах, ні в маркетингу.

**Деталі формул, якорів і сигналів — у `docs/SETTLEMENT_LAYER.md`.**
GDD і поправки фіксують правила; дизайн-док тримає числа.

---

## Поправка №4 — «Прокорм поселення» (2026-08-29)

Рішення власника проєкту.

**Правило.** У поселення є **їжа** — єдиний ресурс, який
виробляється в місті. Вона списується щодня за числом людей у ростері.
Голод не блокує проходження, але тисне з двох боків одразу.

**Навіщо.** Це стимул виходити назовні. Без нього міська петля замкнута сама
на себе: базу можна тримати нескінченно, жодного разу не вийшовши за ворота,
і зв'язка Е6.2 «squad-loop кормит settlement-loop» лишається декларацією.
Голод робить сидіння вдома дорожчим з кожним днем.

**Як тисне:**
- **Напруга.** Голодний день додає Напругу. Гаситься вона Храмом і
  Укріпленнями, а ті будуються за будівельний компонент — який видобувається
  лише у вилазках (Е6.2). Звідси і стимул: пересидіти голод удома не можна,
  тиск знімається лише тим, що принесено ззовні.
- **Просідання бази.** У голодний день позиції працюють гірше: і виробіток,
  і рольовий досвід ідуть із понижувальним множником. База слабшає саме тоді,
  коли їй найпотрібніше бути сильною.

**Розширення закритого списку драйверів.** Поправка №3.5 оголосила список
драйверів Напруги закритим і вимагала, щоб нові системи модулювали
наявні, а не додавали свої. Тут список розширюється **рівно на одне
значення** — `Hunger`, — і це усвідомлена зміна дизайну, а не рефакторинг:
голод не зводиться ні до фонового тику тіру, ні до вибору в квесті, ні до
наслідку загрози. Далі список знову закритий.

**Огородження (несуче).** Їжа **не** конвертується в золото чи компоненти і
**не** входить у жоден рецепт крафту чи проєкт будівництва. Не-бойового
шляху до зростання міста вона не відкриває: зв'язка Е6.2 лишається неторканою.
Їжа — це щоденний такт і ціна за розмір ростера, а не обхідна економіка.

**Що уточнюється в GDD v6:**
1. **Епік 7** (місто): до списку того, що виробляє місто, додається їжа з
   єдиним джерелом — фермами поселення.
2. **Епік 11** (Напруга): до драйверів додається голод.
3. **US-16.2** (м'які фейл-стейти): голод б'є по ресурсах і Напрузі, але
   непрохідності не створює — ферми завжди можуть прогодувати стартовий ростер.

---

## Поправка №4.1 — «Золото — валюта, а не ресурс» (2026-09-20)

Рішення власника проєкту. Уточнює Поправку №4.

**Питання.** Поправка №4 каже: їжа — «єдиний ресурс, який
виробляється в місті». US-7.1 при цьому дає Ринку функцію «дохід золота»,
а Е15 називає золото «валютою загону», а не ресурсом. Чи поширюється
заборона на золото?

**Відповідь власника: ні.** Золото — валюта, і на нього заборона Поправки №4 не
поширюється. Ринок і Вантажний док продовжують давати золото в місті
буквально за US-7.1.

**Що це означає на практиці.** У місті виробляються рівно дві речі: їжа
(ферми) і золото (ринок, док). Матеріали — не виробляються ніде: їхнє джерело
лише вилазки, данжі й нагороди квестів, і це правило Е6.2 лишається
неторканим. Читати список так:

| Що | Чи виробляє місто | Підстава |
|---|---|---|
| Їжа | так, лише ферми | Поправка №4 |
| Золото | так, Ринок і Док | US-7.1, ця поправка |
| Матеріали | **ні, ніде** | Е6.2, Поправка №1 п.2 |

**Чому це не ламає стимул виходити назовні.** Тиск тримається не на золоті,
а на матеріалах і голоді. Золото не годує: їжа не купується (несуче
огородження Поправки №4 — їжа не конвертується). Золото не будує спец-будівлі
й не лікує Напругу: Храм і Укріплення гейтяться будівельним компонентом,
який у місті взяти нема де. Місто, яке ніколи не виходить за ворота,
накопичує золото і бідніє всім іншим.

**Відкритий ризик.** Якщо пізніше з'явиться обмін «золото → їжа» чи «золото →
матеріали» у торговця, заборона Е6.2 обійдеться через гаманець. Такий
обмін вимагає окремого рішення власника, а не появи попутно з торгівлею.

---

## Поправка №5 — «Люди з іменами» (2026-09-05)

> **Статус: ЗАТВЕРДЖЕНА власником 23.09.2026.** На зведення «Поправки №5 і №6
> досі чернетки і чекають твого затвердження» власник відповів: «го».
> Правило старшинства (поправки > GDD) з цієї дати діє.
>
> **Що затверджено, а що ні.** Затверджені рішення власника. Пункти з
> позначками ПЛЕЙСХОЛДЕР, «пропозиція, не рішення власника» і «відкрито»
> такими і лишаються — затвердження поправки їх не вирішує; кожен чекає
> окремої відповіді (список — у 5.7).

Рішення власника проєкту. Закриває чотири питання, поставлені аудитом лупу
(`docs/AUDIT-LOOP.md`), і фіксує склад кампанії. Усе, що позначене
ПЛЕЙСХОЛДЕР або «відкрито», власник ще не вирішив.

### 5.0 Довжина кампанії: дев'яносто діб, близько двадцяти годин

- **Кампанія — ~90 діб**, не 150–200 (§3.1 `SETTLEMENT_LAYER.md` і
  якорі `TensionBalance` перераховуються). Цільова тривалість — **~20
  годин**, тобто **~13 хвилин реального часу на ігрову добу**. Це
  якір для того, скільки змісту зобов'язана нести одна доба.
- Усі числа нижче, прив'язані до діб, дані вже для 90.
- Наслідок для тірів: вимога населення 400/900/1800 при старті 200 і
  прирості ~1.5/добу недосяжна навіть при 200 добах; при 90 — тим паче.
  Умова просування тіру переписується (див. §3.6, відкрито).
- **Старт — до ста людей, і перша ігрова година теж** (власник,
  07.09.2026). Хутір — це менше сотні; так нижня точка дуги «хутір →
  село → слобода → містечко» збігається з нижнім щаблем настільних систем
  управління володінням, де перший щабель — «меньше ста». Якір «200
  человек = стартовое село» (§3.6 `SETTLEMENT_LAYER.md`, `PopulationState`,
  `ReachPopulationAnchor`, формула Досяжності) перераховується; разом з
  ним — вимоги тірів (див. 5.7).

### 5.1 Ростер: двадцять імен, сім-вісім рук

- За кампанію в гравця **~20 іменних напарників** (GDD:488 говорив про 4–6 —
  це рішення переглянуте). Набір поступовий. У кожного — особистий квест,
  що виріс із його першоджерела. Кожен може загинути, зрадити чи піти.
- Одночасно активні: **3 у загоні + 4–5 на позиціях.** Решта — у
  резерві або ще не зустрінуті.
- Це і є дефіцит рук: ростер навмисно менший за суму «пости + загін».
  Симуляційний харнес показав, що без дефіциту міський цикл вироджується
  в штиль (дві політики стають нерозрізнюваними), а з дефіцитом політики
  розходяться: патруль удвічі ріже гірші наслідки, агресивні вибори
  вчетверо частішають кризи.

### 5.2 Усі NPC і всі супротивники — іменні персонажі

**Правило.** Безіменних персонажів у грі немає. **Іменний = має картку**,
а не рядок репліки. Картка — єдиний тип даних для напарника, жителя і
супротивника; інакше іменність ворога стає окремим контент-треком.

- **Населення лишається числом-якорем.** Це не персонажі, а шкала; у фокусі
  гра ніколи не показує безіменного. (Числа «200 → 1800» з першої
  редакції застаріли: старт — до ста, 5.0; пороги тірів — 6.4.)
- **Два яруси за джерелом:**
  - *літературний / міфологічний* — конкретний твір, автор, рік.
    Може бути напарником, великим антагоністом, лідером фракції;
  - *фольклорний / народний, світовий* — традиція, регіон і **збірка в
    суспільному надбанні з роком** (переказ не є джерелом). **Ніколи
    не напарник.** Їх значно менше: частка — ПЛЕЙСХОЛДЕР (запропоновано
    ≤ 1/3 іменного складу), закріплюється тестом за контентом.
- Рядок Поправки №2 «вплив на системи: лише наратив; механік не
  стосується» **скасовується.** Кастинг стає вхідними даними механік:
  учасник інциденту, кого може зачепити криза, ключ анти-повтору, домен
  стану в бою.
- Поле «Першоджерело» обов'язкове для **обох** ярусів і перевіряється тестом
  по всьому контенту — за зразком `HasQuietPath`.
- **Відкрито:** чи можуть іменні жителі тримати міські пости (гірше за
  напарника, але не пусто).

### 5.3 Частота боїв — це число

- **Малі бої: ~1 на 10 діб → ~9 за кампанію.** Завжди уникні двома
  іншими шляхами (Поправка №1). Механічно — інцидент, де обрано кривавий
  шлях. Окремої таблиці зіткнень немає.
- **Великі, що впливають на кампанію: ~1 на 45–60 діб → 1–2 за кампанію.**
  Механічно — криза, де обрано кривавий шлях. Кожен — **авторська
  сцена** з іменним антагоністом, який з'являвся раніше і повернеться пізніше;
  бюджетуються як квести напарників, а не як зіткнення.
- Власник просив **зменшити число боїв** разом із довжиною: числа вище —
  верхня межа, не норма.
- **Пощада має відкладену ціну.** Помилуваний іменний супротивник
  повертається — детерміновано, через накопичувач з його ім'ям — і шкодить
  поселенню. Гравець бачить через шар сигналів, *хто* повернувся.
- Наслідок для бюджету: **~15–25 іменних супротивників** на кампанію, з них
  частина — зрадники з власного ростера.
- Наслідок для бойової системи: ~10 контактів за кампанію утримують
  **малий словник** (ті самі скіли і показані пороги, що в місті) і тим
  паче не окуповують повний тактичний рушій. **Обсяг Епіка 3 при цій частоті —
  окреме рішення, тут не приймається.**

### 5.4 Вихід у вилазку

- Пост при виході **знімається**, не бронюється. Позиція порожніє з усіма
  наслідками: Найгірша полоса, мовчання доповіді, сліпота домену.
- Гравцю про це — **позначка**, а не попап. Попап при зборах рівно **один**,
  з усією інформацією разом: хто йде, які пости порожніють, коли повернуться.
- Наслідок у коді: вихід викликає `Unassign`; повернення автопризначення не
  робить — гравець розставляє заново.
- **Вилазка займає час, і місто в цей час живе** (власник,
  07.09.2026). Це не «перерахувати таймери по прибутті», як у US-1.2 GDD,
  а **повний конвеєр дня на кожну добу відсутності**: інциденти,
  передвісники, ночі, доповіді, цикл поселення — усе йде, наче лідер
  був удома, тільки без його рук. Час стоїть лише всередині локації і в
  бою (US-1.5 лишається). Тривалість вилазки — дорога туди, на місці, дорога
  назад — рахується в добах і показується **до виходу**, у тому самому
  єдиному попапі. Що сталося без тебе — **один звіт по
  поверненні**, а не потік попапів.
- Наслідок, якого GDD не бачив: точка рішення всередині доби
  (`DayStepOrder.PlayerResolution`) за відсутності лідера відповіді від
  гравця отримати не може. Місто зобов'язане розібратися **само**. Пропозиція:
  шлях обирає **заступник** — той, хто стоїть на `council_seat`, за своєю
  карткою (Максим Беркут кривавий шлях не обере; хтось інший — обере).
  Тоді «кого залишити за старшого» — ще одне рішення при зборах, і картка
  персонажа отримує споживача за межами перевірок. Це пропозиція, не
  рішення власника (див. 5.7).
- **Відкрита інверсія**, знайдена дослідженням: жертву кризи обирають
  лише серед присутніх, тому напарник у вилазці **невразливий** для
  кризи. Вилазка зобов'язана бути ризикованою стороною осі «час проти
  ризику», а не сховищем. Форма ризику у вилазці — в рамках Епіка 12.

### 5.5 Матеріали лише ззовні — з застереженням

- Правило Епіка 6.2 / 15 **лишається**: місто компоненти не виробляє.
  Верстак більше не видає матеріали; закріплено тестом за каталогом слотів.
- **Нове: зовнішній вузол ресурсу.** Деякі вилазки можуть заснувати точку
  зовні — наприклад, шахту, — яка дає ресурс **щотижня**,
  обмежено чи необмежено. Вузол обслуговує **іменний житель**, не
  напарник. Вузол може бути **знищений** або **вичерпаний**.
- Зовнішній вузол — ціль для тиску: його можна зруйнувати. Це **новий
  накопичувач Пульсу**, а не новий драйвер Напруги — інваріант 5
  дотриманий.

### 5.6 Що змінюється в коді, у порядку робіт

1. **Єдина картка персонажа**: ім'я, вид джерела, першоджерело,
   атрибути, риси, рани, пам'ять. Заміняє `Companion = Id + StatBlock`.
   Усі три вступні цієї поправки стоять на ній.
2. ~~**Замкнути висячі проводи**~~ — **зроблено 20.09.2026** (див.
   `docs/FIRST_HOUR.md` §4 п. 1). Кров коштує трьох речей одразу: Напруга
   за драйвером `PlaystyleBlood`, рана виконавцю, страх громади —
   `FearState` піднімає поріг Переконання і Торгівлі на кілька діб,
   переживає завантаження і звучить тегом `fear`. Цикл поселення йде в
   конвеєрі через `SettlementCycle` (перший варіант — порт `IDailyCycle` у
   `DefaultSteps(цикл)` — знесений 23.09.2026: він робив цикл бази публічним і
   відкривав обхід конвеєра). Інверсія Поправки №1
   закрита тестом `Blood_IsNotStrictlyBetterThanQuiet` (вісім мутацій).
3. **Учасник інциденту**: поле в `IncidentDefinition`; некризовий наслідок
   перестає повертати порожній `AffectedActorId`; детермінований
   селектор учасника з пам'яттю про те, хто вже виступав.
4. **Вихід і повернення** з `Unassign`; зліпок збереження розширюється на
   ростер, стан «в поле» і календар вилазки (скільки діб лишилося).
5. **Тести змісту**: у кожного іменного є першоджерело; напарник
   ніколи не фольклорний; фольклорних не більше частки.

### 5.7 Що лишається відкритим

- ~~частка фольклорних персонажів — число~~ **закрито 24.09.2026** (Поправка
  №8.2): кап знятий, баланс контентний;
- ~~іменні жителі на міських постах — так чи ні~~ **закрито 24.09.2026**
  (Поправка №8.2): ні, лише напарники;
- ~~форма ризику у вилазці, щоб відсутність не була сховищем~~ **закрито
  24.09.2026** (Поправка №8.3): криза дістає і відсутніх + свій
  ризик-накопичувач вилазки;
- ~~хто обирає шлях інциденту, поки лідер у вилазці~~ **закрито
  24.09.2026** (Поправка №8.3): гравець призначає заступника вручну при
  зборах, не автоматика за `council_seat`;
- ~~обсяг Епіка 3 при частоті «~9 малих, 1–2 великих»~~ **закрито
  24.09.2026** (Поправка №8.1): повний обсяг WL3, не спрощується;
- умова просування тіру при кампанії в 90 діб (§3.6) і нові якорі
  населення при старті до ста — **принцип формули закритий 24.09.2026**
  (Поправка №8.4: населення + ключова будівля + репутація/фракції); числа
  `PopulationState`, `ReachPopulationAnchor`, пороги Досяжності, вимоги
  тірів — як і раніше ПЛЕЙСХОЛДЕР, ставить харнес;
- формат сценарію сцен (кроки і ключі) — де-факто закритий реалізацією:
  портретні сцени з планами і репліками вже працюють у тестовій збірці
  (Поправка №7), окремого рішення не потребує;
- ~~командир орди у фіналі — з якого першоджерела~~ **напрямок
  закритий 24.09.2026** (Поправка №8.2): Сумангуру Канте, епос про Сундіату;
  фінальний кастинг чекає перевірки видання до 1930 року;
- ~~мова документів і коментарів у коді~~ **закрито 24.09.2026** (Поправка
  №8.5): перекладаються на українську окремим проходом.

### 5.8 Кінематографічні сцени — портретні, з планами і ефектами

**Рішення власника:** сцена — це **портретна діалогова сцена з планами
і репліками**, плюс **ефекти** (вибухи і подібне). Не камера в
ізометричному світі.

Форма, сумісна з архітектурою проєкту: сцена — це **дані**.
Декларативний сценарій із кроків: *план* (хто в кадрі і як — великий,
подвійний, порожній), *репліка* (хто і ключ тексту), *пауза*, *ефект*
(ключ VFX/звуку), *перехід*. Асистент пише сценарій текстом, ядро зберігає
і перевіряє його тестами, `Game.Gameplay` відтворює інтерпретатором
поверх штатних засобів рушія. Той самий принцип, що й у шару сигналів: ядро
віддає ключі і теги, рушій рендерить.

Що це дає: сцену можна **поставити і перевірити без редактора** (порядок
кроків, що всі ключі існують, що в кожного учасника є картка і
портрет); ефекти і портрети підбираються під ключі з доступних асетів
(портрети — музейний open access, див. `docs/ASSETS.md`, розділ «Портрети
іменних персонажів»). Інтерпретатор будується
один раз — це робота в редакторі.

### 5.9 Перша ігрова година: одразу до справи

**Рішення власника:** гра починається **одразу з двох інцидентів і першого
великого бою, від якого залежить доля** — а не зі спокійного знайомства
з містом.

- Перша година при ~13 хвилинах на добу — це **перші 4–5 діб**. У них
  зобов'язані вміститися: два інциденти (у кожного тихий і кривавий шлях із
  показаними порогами) і одна велика авторська сцена з іменним
  антагоністом, який повернеться.
- Це відповідає на знахідку харнеса: за поточного балансу перший інцидент
  приходить на 9-у добу, тобто **приблизно через дві години гри** — для
  20-годинної гри це 10 % кампанії без жодної події. Відкриття ставиться
  **авторськи**, не чекає накопичувачів.
- Відкриття зобов'язане вчити головному лупу (Поправка №3), а не бою: два
  інциденти — міські; великий бій — виняток, а не норма, і його
  ненасильницький шлях видно з першої сцени.
- **Форма відкриття обрана власником: «Перевал»** — за повістю Івана
  Франка «Захар Беркут» (1883, суспільне надбання). Кастинг, вузли по
  добах, приймальний чеклист на двадцять рядків і порядок збірки — у
  `docs/FIRST_HOUR.md`. Перша збірка зрізу — текстова, в `tools/`.
- §17.1 GDD переписується під це повністю.

---

### 5.10 Мова гри — українська

**Рішення власника (23.09.2026):** «Почему вся игра на русском если должна
быть по украински».

- Усе, що бачить гравець, — українською: репліки сцен, стрічка і заголовки
  села, сигнали, меню, консольна збірка `tools/Alpha.Play`, пам'ятка
  тестеру.
- Імена — за українським оригіналом повісті: Захар Беркут, Максим,
  Мирослава, Тугар Вовк.
- Не перекладаються ключі й ідентифікатори (`scene.neighbour.offer`,
  `storehouse_dock`). Документи і коментарі в коді лишаються мовою
  репозиторію — *пропозиція, не рішення власника*.
- Чому було російською: мова гри ніде не була записана, і тексти писалися
  мовою репозиторію. Цей пункт закриває прогалину.
- **Стан коду на 23.09.2026: не перекладено.** Текст розкиданий по
  кількох сотнях рядкових літералів (Gameplay, консольна збірка, частина —
  в ядрі). Порядок: спочатку весь видимий текст — за ключами в одну таблицю на
  збірку, потім перекладається таблиця. Інакше наступний екран знову напишеться
  російською.

## Поправка №6 — «Місто відповідає» (2026-09-23)

> **Статус: ЗАТВЕРДЖЕНА власником 23.09.2026.** На зведення «Поправки №5 і №6
> досі чернетки і чекають твого затвердження» власник відповів: «го».
> Правило старшинства (поправки > GDD) з цієї дати діє.
>
> **Що затверджено, а що ні.** Затверджені рішення власника. Пункти з
> позначками ПЛЕЙСХОЛДЕР, «пропозиція, не рішення власника» і «відкрито»
> такими і лишаються — затвердження поправки їх не вирішує; кожен чекає
> окремої відповіді (список — 6.6).

Рішення власника:

> «чому один? А в нас немає інших будівель? Усі ж мають шось коштувати»

Клапан — не одна будівля. Будуються **всі** будівлі US-7.1, і **кожна чогось
коштує**.

> «В вилазках можуть бути люди, чи вони можуть прийти по івенту чи рішенню
> в місті, чи покинти його»

Населення рухливе в обидва боки, і село росте **від людей** — це відповідь на
питання «от людей, от зданий или от связей».

### 6.0 Чому це перше — замір

Харнес, 16 кампаній по 200 діб на новій моделі статів (23.09.2026):

- усі 16 кампаній закінчуються полосою «Раскол»;
- Хороша полоса випадає один раз за кампанію, Найкраща — жодного разу;
- Напруга піднята на 680–1010 одиниць, опущена на 8: близько ста до одного;
- процвітання в мудборді дорівнює `тир − 1`, а тір за кампанію не змінює жоден
  рядок коду; населення не росте ніколи;
- з п'яти понижувальних драйверів закритого списку чотири (`CouncilRaid`,
  `CouncilEdict`, `TempleAura`, `Fortifications`) не викликаються ніде.

Місто вміє тільки погіршуватися. У лупу є тиск і немає клапана — це і
є «місту нема чим відповісти на добрі дії гравця».

### 6.1 Будівлі: усі будуються, усі коштують

Список — US-7.1 GDD. Ціни і строки — **ПЛЕЙСХОЛДЕР**, виставляються харнесом.
Строк будівництва йде в добах із п'ятьма видимими стадіями (US-7.3).

| Будівля | Ціна | Ефект | У що входить |
|---|---|---|---|
| Лазарет | золото | відкриває ліжко лазарету | пост `infirmary_bed`, лікування |
| Майстерня | золото | відкриває верстак | пост `workshop_bench` |
| Склад | золото | відкриває вантажний док | пост `storehouse_dock`, дохід золота (№4.1) |
| Зал ради | золото | відкриває місце в раді і дії ради | пост `council_seat`, §6.2 |
| Ринок | золото + матеріали | відкриває ринок | пост `settlement_market`, дохід золота (№4.1) |
| Таверна | золото + матеріали | пришвидшує приріст, приводить прибульців | §6.3 |
| Храм | золото + матеріали | щодоби знижує Напругу | драйвер `TempleAura` |
| Укріплення | золото + матеріали | щодоби знижують Напругу | драйвер `Fortifications` |
| Зброярня | золото + матеріали | ефект чекає спорядження (Е4) | — |
| Лабораторія | за квестом (GDD) | ефект чекає аугментів (Е6) | — |

Матеріали місто не виробляє (Е6.2, №4.1) — тому просунуті будівлі
замикають петлю «вилазка → матеріали → будівництво → місто спокійніше». Саме цей
стик «поселение ↔ мир» дослідження жанру назвало місцем, де зв'язка
розімкнута.

**Стартовий хутір (пропозиція, не рішення власника):** Зал ради і Склад —
у громади вже є старший і комора. Усе інше будується.

### 6.2 Дії ради

- **Облава** — драйвер `CouncilRaid`: разове зниження Напруги, ціна
  золотом, відкат за `RaidCooldownDays`. Вимагає Зал ради.
- **Прийом переселенців** — «решение в городе» зі слів власника: люди
  приходять, ціна — їжа, якою тепер треба годувати більше ротів.
- Указ і Дипломатія — чекають фракцій (Е2).

### 6.3 Люди приходять і йдуть

Приплив: природний приріст (малий), Таверна (пришвидшення), вилазка (знахідка
на Хорошій чи Найкращій полосі), подія (прибульці), рішення ради
(переселенці). Відтік: криза (вже є), голодні доби, страх громади.

Що чутно, а що ні (уточнення при реалізації, 23.09.2026). Сигнал на
кожну нову людину перетворився б на шпалери, тому:
- **тихо**: природний приріст і таверна — по людині на добу;
- **чутно завжди**: прихід за рішенням ради, з вилазки і за подією;
  будь-який відхід людей — це наслідок, який гравець зобов'язаний помітити; зміна тіру;
- **чутно на переході**: коли людність переходить в іншу полосу
  (`PopulationState.CrowdBand`) — рівно за інваріантом 4, німого переходу
  полоси не буває.

Реалізація: `Core/Base/Buildings/` — каталог будівель, міські роботи, кроки
`CityWorksStep` (Construction, 100) і `PopulationStep` (Population, 300),
політика «рачительний господар» для приймання. Тести — `CityWorksTests`,
одинадцять мутацій спіймані.

Знайдений і закритий супутній дефект ядра: дробові залишки всіх драйверів
Напруги накопичувалися в ОДНІЙ змінній. Дренаж храму −0.5 тонув у фоновому
тику +1.0 — сумарна Напруга була вірною, але журнал записував зниження
тику, а храму нуль. Тепер залишок у кожного драйвера свій і входить у зліпок.

### 6.4 Тір росте від людей і ключових будівель

Поріг тіру — населення **і** ключова будівля (GDD US-7.6 називає комбінацію).
**Пропозиція, ПЛЕЙСХОЛДЕР:** тір 2 — 150 людей і Таверна; тір 3 — 250 і
Храм чи Ринок; тір 4 — 400 і Укріплення. Мета налаштування: за хорошої гри
тір 2 близько тридцятої доби, тір 3 близько шістдесятої.

Зміна тіру чутна (сигнал) і видна (мудборд: процвітання = тір − 1). Тір
піднімає і тиск — так за GDD: вищий тір, серйозніші події.

### 6.5 Приймання

- Клапан існує в числах: за політики «хорошая игра» кампанія не зобов'язана
  закінчуватися Розколом, а понижувальна сторона одного порядку з підвищувальною.
  Стверджується тестом харнеса.
- Список драйверів не розширюється (інваріант 5): усе зниження — через
  наявні `CouncilRaid`, `TempleAura`, `Fortifications`, `EventOutcome`.
- У будівництва є якір (доба), споживач (ефект по завершенні) і сигнал
  (п'ять стадій і репліка про готовність) — інваріант 6.

### 6.6 Що лишається відкритим

Затвердження поправки (23.09.2026) не вирішує цих пунктів — вони чекають
окремої відповіді власника:

- ціни і строки будівництва всіх будівель — ПЛЕЙСХОЛДЕР (6.1), ставить харнес;
- ~~стартовий набір хутора: Зал ради і Склад — пропозиція (6.1)~~
  **закрито 24.09.2026** (Поправка №8.4): відхилено, старт без будівель;
- пороги тірів «150 + Таверна / 250 + Храм або Ринок / 400 + Укріплення» —
  **принцип формули закритий 24.09.2026** (Поправка №8.4: + репутація/фракції);
  конкретні числа лишаються ПЛЕЙСХОЛДЕРом (6.4), ставить харнес;
- числа припливу і відтоку людей, відкат прийому переселенців — ПЛЕЙСХОЛДЕР
  (`CityBalance`);
- ~~політика «рачительного господаря» (`Steward`) — чи ставити вільних на
  відкриті пости автоматично в самій грі~~ **закрито 24.09.2026**
  (Поправка №8.4): опція складності/автоматизації, не жорстке так/ні.

## Поправка №7 — «Тестова збірка: усі механіки одразу» (2026-09-23)

> **Статус: ЧЕРНЕТКА за рішеннями власника від 2026-09-23. У силу вступає
> після затвердження власником.** До цього правило старшинства (поправки >
> GDD) на неї не поширюється.

Рішення власника, дослівно:

> «Дай мені білд де я зможу оцінити наші механіки усі, умовно першу годину»

> «А ДАНЖ, ЗРАДА ТА УСІ ІНШІ МЕХАНІКИ???? Нащо тестовий білд якщо немає усіх
> механік та динамік?»

> «Тестову версію з усіма механіками та динамиками та текстурами давай, текст
> який сам згенеруєш, вибори та наслідки також»

> «Так, це все має бути одна гра, це механіки однієї гри, шукай де розриви
> потім як закінчиш»

### 7.1 Одна збірка — усі механіки і динаміки

Тестова збірка — це Unity-білд, у якому власник за «умовно перший
час» чіпає руками **кожну** механіку проєкту, а не зріз одного шару.
Окремих демо-сцен на механіку не робиться: усе йде в одній партії, і
механіки впливають одна на одну так само, як у грі.

- **Економіка і місто входять у світ зрізу.** Закриває питання «подключать ли
  производство в `tools/Shared/SettlementWorld`» (CLAUDE.md, «Мост
  производства»; FIRST_HOUR, рядок 14): так. Час іде тільки через
  `SettlementCycle`.
- **Харнес переїжджає на той самий світ** — правило FIRST_HOUR §4 п. 6 (зріз і
  замір будують світ однаково) не скасовується. Якщо числа темпу зсунуться, це
  фіксується заміром, а не підгонкою.

### 7.2 Архівна лінія повертається як джерело механік

Гілка `claude/gifted-northcutt-615bf1` (24 коміти, червень–серпень 2026:
тактичний бій, данжі, лут, спорядження і крафт, драма ростера, лояльність і
зрада, квести, фінальний бій, кампанія і сейви) більше не вважається
викинутою. Її механіки **переносяться** на поточну модель персонажа (Е2:
атрибути, скіли, трейти, шрами, перки через `StatResolver`) і підключаються до
міського лупу. Саме переносяться, а не зливаються: стара модель (`StatType`)
видалена, і злиття як є не збереться.

`docs/DESIGN.md` і `docs/BALANCE.md` лишаються історичними. Там, де
архівна механіка суперечить поправкам №1–№6, перемагають поправки, а суперечка
записується сюди.

### 7.3 Текст пише асистент

Репліки, описи подій, варіанти вибору і їхні наслідки для тестової
збірки пише асистент, українською (Поправка №5.10). Статус такого тексту —
**чорновий текст асистента**: він потрібен, щоб механіки можна було оцінити,
і не вважається каноном, поки власник його не поправить. Правило Поправки №2
(персонажі — із суспільного надбання) діє і для нього.

### 7.4 Вибори і наслідки

Кожна подія збірки дає гравцю вибір, і в кожного варіанта є
наслідок, який гравець **бачить**: у стрічці, у сцені, на людях, у ресурсах
або в поведінці міста в наступні доби. Вибір без видимого наслідку
вважається дефектом збірки.

### 7.5 Одна гра, а не набір механік; аудит розривів

Бій, данжі, лут, зрада, місто і сцени — механіки **однієї** гри.
Кожна перенесена механіка зобов'язана мати вхід із міського лупу і вихід
назад у нього (данж починається вилазкою і повертає людей, рани і
матеріали; зрада росте з рішень у місті й б'є по місту).
Механіка без входу або без виходу — **розрив**.

Після збірки проводиться **аудит розривів**: які механіки не живлять одна
одну, де вибір не змінює нічого далі по ланцюжку, які числа ніхто не
читає. Результат — список з місцем у коді і пропозицією, власнику.

### 7.6 Що лишається відкритим

- тестова збірка ≠ остаточний темп першої години: механіки стиснуті в
  перші доби, щоб їх можна було оцінити. §5.9 («перший час вчить
  міському лупу, великий бій — виняток») лишається проєктом справжньої
  першої години — *пропозиція, не рішення власника*;
- архівні механіки, які суперечать поправкам, і як вони переосмислені —
  список ведеться тут по ходу перенесення.
- **правило влучання в бою.** GDD US-3.3 і модель бою в CLAUDE.md —
  «надійний %» (кидок d100 з показаним шансом, так працює архівний код);
  інваріант 1 і §3.3 — «жодних прихованих кидків», інваріант 8 — «скил ≥
  порога, порог показан». Поправки старші за GDD, але відсоток показаний, тобто
  кидок не прихований — однозначної відповіді в документах немає. У збірці є
  обидва правила, обираються при новій грі; за замовчуванням відсоток.
  Рішення — за власником після гри;
- обсяг Епіка 3 при ~9 малих і 1–2 великих боях (§5.7) збіркою **не
  вирішується**: тактичний бій у ній є, щоб його оцінити, а не тому,
  що обсяг обраний.

### 7.7 Будівництво в тестовій збірці — один день

**Рішення власника (24.09.2026):** «на момент тесту механік та геймплей
лупу усі будівлі 1 день».

- У тестовій збірці **кожна** будівля будується за одну добу: замовлення вранці —
  готово до наступного ранку, пост відкривається одразу. Інакше за п'ять діб
  ланцюжок «будівництво → пост → крафт/ефект будівлі» не встигає дійти до
  гравця, і механіка не оцінюється.
- Це налаштування **тестової збірки**, а не баланс кампанії: проєктні строки
  будівництва (п'ять стадій по добах, Поправка №6.1, ПЛЕЙСХОЛДЕР) у даних
  лишаються і вмикаються звичайною новою грою.

### 7.8 Вибори в діалогах і квестах — механіка, яку тестуємо

**Рішення власника (24.09.2026):** «Выборы в диалогах или квестах должны
влиять это же тоже механики которые нужно протестировать как и
предательство. Это все должно быть в первом билде, должны быть
предсавленны все механики какие есть».

- У портретних сценах з'являється **вибір репліки**. Варіант може вимагати
  перевірку (поріг показаний заздалегідь, інваріант 8) і **змінює світ**: лояльність
  напарників, ставлення фракцій, готовність, сюжетні прапорці, Напругу
  через закритий список драйверів. Наслідок видно (7.4).
- Квести і **глави особистих арок напарників** — з виборами і наслідками;
  арка не лише відкривається, а й програється.
- **Зрада грається повністю**: передвісники недовіри → розмова, у
  якій його можна відвернути чи спровокувати → відхід → повернення
  ворогом. Усе в першій годині тестової збірки.
- У збірці є **журнал механік** для тестера: які механіки вже
  зустрілися в цій партії, які ще ні і як їх викликати.

### 7.9 Темп Напруги і великий бунт у тестовій збірці (ЧЕРНЕТКА, 24.09.2026)

> **Статус: ЧЕРНЕТКА за рішенням власника від 24.09.2026.** Числа — плейсхолдер
> тестової збірки (див. 7.7/7.8 — той самий статус), не баланс кампанії.

**Проблема (замір 24.09.2026, HEAD 2e115da):** за 25 діб тестової збірки
Напруга не піднімалась вище ~95 з 1000 при порозі першої полоси 200 —
ні зміна полоси, ні великий бунт не траплялись НІ В ОДНОМУ прогоні. Причина —
масштаб: за першу годину всі драйвери разом дають десятки очок (навіть
найгірша гра — ~40 до доби 5, ~120 до доби 12), а кампанійна шкала
розрахована на 90 діб; Облава ради (бот замовляє її на кожній
готовності) з'їдала і це зростання. Природний накопичувач кризи
(`CrisisPressureSource`, поріг 120, копить тільки з «Розпалу») не міг навіть
почати.

**Рішення власника:** «За 5 днів пожежа, потім інші механіки. Так для тесту
ввімкни що за 15 днів перша смуга за 20 друга і за 25 криза? Чи це занадто
швидко і треба хочаб 30-40 днів?» Провідний асистент у тому ж обговоренні:
15/20/25 — вірний темп саме для тестової збірки; між другою полосою і
кризою зобов'язаний стояти «Розпал» (~доба 22-23) з триступеневою драбиною
передвісників і вікном милосердя — інакше криза вдарила б німо (інваріант 4).
Підсумкова ціль для гравця, який не замовляє облав і не будує Храм/
Укріплення («безрукий еталон»): Ропіт ~доба 15, Бродіння ~доба 20,
Розпал ~доба 22-23, бунт ~доба 25.

**Що зроблено (`Core/Session/TestBuildTensionPace.cs`, прапорець
`NewGameOptions.TestBuildTensionPace`, дефолт true, той самий прийом, що
`TestBuildOneDayConstruction`, Поправка №7.7):**

- фоновий тик за тиром (єдине джерело пасивного тиску, US-1.3)
  заданий явно: 10/12/14/16 за добу для тирів 1–4 замість кампанійних
  1/2/4/7. Не множником: з множником село (тир 2) тікало б удвічі швидше
  за хутір, і жодна Облава не встигала б. Дренаж Храму/Укріплень
  масштабований ×10 — тим самим відношенням, що тик хутора, інакше подавлення
  нічого не важило б;
- пороги полос тестової збірки — 145/265/305/380 (Ропіт/Бродіння/Розпал/
  Злам) замість кампанійних 200/400/600/800, нерівномірно: після Ропоту
  частішають інциденти вулиці і ночі (їхня ставка росте з полосою), тому
  Бродіння→Розпал проходиться швидше, ніж Спокій→Ропіт;
- поріг накопичувача кризи (`CrisisPressureSource`) стиснутий до 9 (кампанійний
  — 120); ставки накопичення (5/12 за тик) і вікно милосердя між третьою
  почутою ступінню і бунтом (`PulseBalance.CrisisGraceDays` = 1 замість
  кампанійних 3, але не 0 — R2 вимагає хоча б добу, інакше «почута
  ступінь = той самий день, що бунт» читалась би як німа криза) — окремі
  важелі, теж стиснуті, але із **закритого списку** (інваріант 5: тільки
  наявні драйвери, жодних нових);
- відкат кризи тестової збірки — 5 діб замість 30
  (`TestBuildTensionPace.CrisisCooldownDays`). Знайдено перевіркою 25.09.2026:
  з порогом 9 заряд відновлюється за дві фази, і драбина «площі»
  звучала знову наступного ранку після бунту, а з відкатом 30 діб за нею нічого не
  йшло — передвісник брехав (SETTLEMENT_LAYER §5.1, правило 4). Тепер
  повторна драбина чесно веде до другого бунту, якщо місто не
  заспокоїли (еталон: бунти на добу 25 і 30; шкідник — 23 і 28, обидва рази
  гине напарник; дефузер — жодного);
- усі інші поля балансу (вибори квестів, кров, голод, Облава) —
  буквально кампанійні значення: власник просив стиснути темп, а не
  переважити ці драйвери.

**Що лишається кампанійним без змін:** `tools/Alpha.Sim` (калібрування
90-добової кампанії) і `Game.Core.Sim.CampaignSimulator`/
`CampaignPacingTests`/`CityWorksTests`/`FirstHourWorldTests` звуть
`FirstHourWorld.Build` без прапорця (чи явно з `false`) — їхній `BalanceConfig` не
бачить змін узагалі (перевірено: `summary.csv` з `Alpha.Sim` побайтово
однаковий до/після, `Off_CampaignProfileUnchanged` проходить). **Уточнення:**
траса `trace-*.csv` — не побайтово ідентична: коміти домену в
передвісниках (b5a9a4a/7d9c260) додають `TranslateReport.DomainTagFrom` у
цикл `Signals` безумовно, для будь-якої сесії, і кожен рядок `signal.*`
отримує адитивне поле `;domain=<тег>` (чи `;domain=` без тега) —
формат логу змінюється, числа і рішення конвеєра — ні.

**Заміри (бот-прогони `BotRunner`, 30 діб, детермінований `HitRuleKind.
Threshold`):**

| Профіль | Ропіт | Бродіння | Розпал | Бунт |
|---|---|---|---|---|
| Еталон — ситий домосід (`HomebodyPolicy`: мирно, без вилазок, без Облав/Храму/Укріплень) | доба 15 | доба 21-22 | доба 21-23 | доба 25 і 30 |
| Дефузер — той самий домосід з рутиною ради (Облава, Храм, Укріплення) | доба 23 | — | — | жодного за 30 діб |
| Голодний — бот з безперервними вилазками (пости порожніють, голод з доби 8) | доба 12 | доба 17-18 | доба 17-19 | доба 21-23 |
| Кривавий (`BloodyPolicy`, без ради) | доба 14 | доба 21 | доба 21 | доба 24 |
| Шкідник (`NeglectPolicy`: криваво, порожні пости, без патруля) | доба 10 | доба 15 | доба 15 | доба 19 |

**Перепідбір 25.09.2026.** Перший підбір (24.09: тик ×3, пороги
115/250/300/450) спирався на Pacifist, який з доби 8 голодає
(+8 Напруги за добу) — темп тримався на голоді. Перевірка справжнім
Unity-прогоном (`-autoplay-long`) показала: ситий водій туру побачив
Ропіт лише близько тридцятої доби, бунту — жодного. Еталоном став ситий
домосід — модель живого тестера; голод, кров і порожні пости тепер
прискорюють, а не задають темп. Там же знайдено й закрито: прапорець темпу не жив у
сейві — «Продовжити» будувало світ з типовими опціями, і партія з
кампанійним темпом (перемикач на титулі) мовчки продовжувалась у
тестовому. Тепер `tensionPace` у заголовку слепка; слот з іншим темпом
перебудовує світ тим самим шляхом, що «Продовжити».

Скриптована пожежа доби 5 (`ForcedCrisisSource`, R10 §TEST_BUILD.md)
лишається без змін — це окрема, перша за рахунком криза тестової
збірки; природний бунт на площі — другий, пізніше, і це дві різні речі
для гравця (журнал механік їх не плутає — див. нижче).

**Що бачить гравець (Integrate-фаза, 24.09.2026):**

- драбина передвісників називає домен на ступенях 2-3 (SETTLEMENT_LAYER
  §5.1 правило 4): "forewarn.level2/3" — раніше безликі для БУДЬ-ЯКОГО джерела
  ("щось готують. скоро.") — тепер читають {domain} з тега джерела тиску
  (`SignalComposer` уже кладе "domain:"+DomainTag у теги передвісника;
  `GameSession.TranslateReport`/`DomainTagFrom` дістає тег і кладе окремим
  аргументом "domain", `ScreenText.EventLine` уже вміє його підставляти —
  той самий прийом, що "domain.road"/"domain.craft"). Подієвий Key не
  змінюється ("forewarn.levelN" спільний для всіх джерел) — розрізняти
  джерело (площа/вулиці/ніч/Тугар) читачеві дозволяє саме {domain},
  а не новий ключ;
- репліка зміни полоси «Злам» переписана — вона звучить як край бунту, не як
  ще одне «змінюється» (раніше буквально повторювала текст проміжного
  сигналу);
- журнал механік отримав два нових пункти — «Настрій міста рухається»
  (зсув полоси) і «Великий бунт на площі» (природна криза); наявний
  пункт про скриптовану пожежу доби 5 перейменовано, щоб не плутати з
  новим;
- титульний екран отримав перемикач «Темп шкали Напруги: тестовий/
  кампанійний», за замовчуванням тестовий (той самий прийом TabButton, що правило
  влучання);
- автопрогон отримав прапорець `-autoplay-long`: після звичайного туру веде
  вільну гру далі без втручання (без облав/Храму/Укріплень) зі
  знімками на кожну зміну полоси, кожну ступінь передвісника кризи й на
  саму розв'язку бунту, до стелі доби 32.

## Поправка №8 — «Відповіді по §5.7 і §6.6» (2026-09-24)

> **Статус: ЧЕРНЕТКА за рішеннями власника від 2026-09-24.** Зібрані опитуванням
> по чотири питання за раз (інструмент AskUserQuestion, три раунди). У силу
> вступає після затвердження власником — як і попередні поправки.

Закриває дванадцять пунктів із «Що лишається відкритим» (§5.7, §6.6) і
списку «Відкрито» з CLAUDE.md. Три пункти рішенню не підлягали і
лишаються відкритими усвідомлено (див. 8.6).

### 8.1 Бій

- **Обсяг Епіка 3 — повний.** Тактичний рушій WL3 (пул AP, укриття,
  overwatch, шар станів) не спрощується, попри рідкісність боїв
  (~9 малих + 1–2 великих за кампанію, Поправка №5.3). Власник: «Лишити
  повний обсяг WL3».
- **Правило влучання (§7.6) лишається відкритим усвідомлено** — власник: «Після
  тесту скажу». Рішення переноситься на момент, коли власник пройде
  тестову збірку руками. Default збірки (відсоток) не змінюється до того часу.

### 8.2 Кастинг

- **Числова стеля частки фольклорних персонажів знімається** (був
  «не больше 1/3», Поправка №5.2). Баланс літературного/фольклорного ярусу —
  контентний, «на глаз», без тесту-охоронця на конкретне число. Власник:
  «Взагалі всі персонажі іменні, персонажів СВІТОВОЇ культури у паблік
  домейні і не тільки популярних дуже багато» → «Кап прибрати».
- **Особистий квест лишається в усіх ~20 іменних напарників** — Поправка №5.1 не
  змінюється. (Проміжна репліка власника про «10 квестів» уточнена ним
  же: «квести — усім ~20».)
- **Іменні жителі НЕ тримають міські пости** — лише напарники.
  Власник: «Ні, лише напарники». Пункт §5.2 «чи можуть іменні жителі
  тримати міські пости» закритий: ні.
- **Командир орди у фіналі (FIRST_HOUR §6) — Сумангуру Канте (Сумаоро),
  цар Сосо**, антагоніст епосу про Сундіату (Малі, усна традиція,
  систематично записувалася колоніальними дослідниками з кінця XIX ст.).
  Власник: «Давай шось з африки, чи латинської америки» → «Сумангуру Канте
  (Африка, епос про Сундіату)». **Кастинг не закритий до перевірки**
  конкретного видання до 1930 року для поля «Першоджерело» — окреме
  завдання, за зразком перевірки ліцензій у `docs/ASSETS.md`.

### 8.3 Вилазка і відсутність лідера

- **Шлях інциденту, поки лідер у вилазці, обирає не автоматичний
  заступник.** Пропозиція документа (§5.4: заступник на `council_seat`
  вирішує за своєю карткою) відхилена. Замість цього: **гравець вручну
  призначає заступника** в попапі зборів на вилазку. Власник: «Гравець
  призначає заступника вручну при зборах».
- **Форма ризику у вилазці — обидва механізми одразу.** Криза може обрати
  жертву і серед відсутніх (партія в полі втрачає «иммунитет
  отсутствия»), і вилазка отримує власний ризик-накопичувач, не
  пов'язаний з міською кризою. Власник: «Обидва разом».

### 8.4 Місто

- **Стартовий хутір — без будівель.** Пропозиція §6.1 («Зал совета и Склад
  уже есть бесплатно») відхилена. Усе, включно із Залом ради і Складом,
  будується з нуля. Власник: «Зовсім без будівель на старті».
- **Умова просування тіру — три фактори.** До формули «население +
  ключевое здание» (§6.4) додається репутація/фракції (Епік 10), як і
  задумував GDD US-7.6. Власник: «Ще й репутація/фракції». Конкретні
  числа населення за тірами лишаються ПЛЕЙСХОЛДЕРом, їх ставить харнес — питання
  було лише про сам принцип формули.
- **Політика «рачительного господаря» (Steward) у реальній грі — опція
  складності/автоматизації**, не жорстке так/ні. Власник: «Як опція
  складності».

### 8.5 Технічний борг і мова репозиторію

- **Інваріант 2 (G11: три накопичувачі Пульсу читають одну змінну)** —
  технічний баг ядра, не творче рішення. Лагодиться **разом з
  рештою технічних фіксів цього блоку**, не окремим терміновим патчем.
  Власник: «Разом з іншими фіксами цього блоку».
- **Мова документів і коментарів у коді перекладається на українську**
  (§5.10 було «пропозиція, не рішення власника» — тепер рішення).
  Власник: «Перевести на українську» → «Зараз, окремим проходом». Переклад
  іде окремим проходом, починаючи одразу після цієї поправки: спочатку
  доки рівня джерела істини (`CLAUDE.md`, `docs/GDD.md`,
  `docs/GDD_AMENDMENTS.md`, `docs/SETTLEMENT_LAYER.md`), потім решта
  `docs/*.md`, потім коментарі в коді (`Assets/_Project/Scripts`,
  `tools/`) окремим, обережнішим проходом — код чіпається бережніше
  документації.

### 8.6 Усвідомлено не вирішувалося

- Правило влучання в бою (див. 8.1) — чекає, поки власник пройде тестову
  збірку.
- Інваріант 2 (див. 8.5) — технічний баг, не питання власнику; пріоритет
  зафіксований, саме виправлення — окреме завдання.
- Числа: ціни і строки будівництва будівель (§6.1), пороги населення за тірами
  (§6.4), числа припливу/відтоку людей (§6.3) — усе це як і раніше
  ПЛЕЙСХОЛДЕРи, які налаштовує харнес, а не пряма відповідь власника;
  формули/принципи за ними закриті цією поправкою (8.4), самі числа — ні.

### 8.7 Що знімається з «Що лишається відкритим»

Із §5.7: частка фольклорних; іменні жителі на постах; форма ризику у
вилазці; хто обирає шлях інциденту за відсутності лідера; обсяг Епіка 3;
умова просування тіру і якорі населення (принцип, не числа); командир
орди (напрямок, не фінальний кастинг); мова документів і коментарів.

Із §6.6: стартовий набір хутора; пороги тірів (принцип, не числа); політика
Steward у грі.

Лишаються відкритими в §5.7/§6.6 як є: ціни і строки будівництва (числа),
числа припливу/відтоку людей (числа), формат сценарію сцен (де-факто закритий
реалізацією — портретні сцени вже працюють у тестовій збірці, окремого
рішення не потребує).

## Поправка №9 — «Драбина кризи не обіцяє того, що забороняє відкат» (ЧЕРНЕТКА, 2026-09-25)

> **Статус: ЧЕРНЕТКА — пропозиція асистента, не рішення власника.**
> Знайдено під час підбору темпу тестової збірки (§7.9): там те саме обійдено
> відкатом 5 діб. Тут — кампанія. Поведінку вже реалізовано, бо вона виконує
> наявне правило, а не змінює баланс (дні криз ті самі — замір нижче);
> затвердження потрібне для формулювання §3.2 і для вибору між двома
> варіантами (9.3). Первісно записано як «Поправка №8»; перенумеровано, бо
> №8 паралельно зайняли відповіді власника по §5.7/§6.6.

### 9.1 Розрив

`SETTLEMENT_LAYER.md` §3.2 видає передвісника за часткою заповнення
накопичувача, §5.1 правило 4 вимагає «Передвісник не бреше», §5.2 — «Злам →
криза протягом 1–3 діб». Накопичувач кризи (`CrisisPressureSource`: поріг
120, відкат 30 діб, 5/12 за тік на Розпалі/Зламі, тіків два на добу) копить
і під час відкату. Після удару заряд і драбина скинуті, але за 3–5 діб
драбина знову доходить до третього щабля («скоро») — а наступну кризу
відкат пускає лише через 30. Третій щабель висить ~25 діб і нічого не
означає.

Замір `CampaignSimulator` (12 кампаній: Passive/PatrolEveryNight/
AggressiveChoices × тири 1–4, 200 діб, усі доходять до Зламу):

| | до | після |
|---|---|---|
| дні криз | кожні 30 діб (Passive t1: 58, 88, 118, 148, 178) | ті самі, в усіх 12 кампаніях |
| криз за 200 діб | 5–6 | 5–6 |
| третій щабель → криза, перша криза | 3 доби | 3 доби |
| третій щабель → криза, наступні | **25 діб** | 3 (з патрулем 4) |

`tools/Alpha.Sim` (5 політик × 90 діб): до природної кризи не доходить
жодна політика — Напруга не вища за Бродіння, у зведенні «криз 1» — це
скриптована пожежа доби 5. Траси до/після побайтно однакові.

### 9.2 Що зроблено (варіант А — утримання передвісника)

Заряд копиться як раніше; змінюється лише момент, коли гравець ЧУЄ
драбину. Після кризи, що спрацювала, її драбина мовчить, доки до кінця
відкату лишається більше, ніж вікно на реакцію (`PulseBalance.CrisisGraceDays`
= 3) плюс довжина самої драбини (`PulseBalance.CrisisLadderLeadDays` = 2:
три щаблі по одному на добу в того, хто спить). Драбина починається за 5
діб до кінця відкату, третій щабель чути за 3 (з патрулем 4) доби до
удару, криза б'є того самого дня, що й раніше. Перша криза (відкату ще не
було) та інші загрози (вулиця, ніч, Тугар — короткий відкат) не зачеплені.
Код: `PressureTrack.HoldsForewarnings`, виклик у `WorldPulse.Advance`.
Охоронці: `CampaignPacingTests.Pacing_CrisisLadder_Level3_AlwaysLeadsToTheCrisis`,
`WorldPulseTests.Pulse_CrisisLadder_NeverPromisesACrisisTheCooldownForbids`
(удар рівно через відкат, кожен третій щабель веде до удару не пізніше ніж
за 5 діб), `Pulse_NonCrisisSource_ForewarnsDuringCooldown_AsBefore`,
`Pulse_FirstCrisis_IsNotHeldBack_ByAnUnstartedCooldown`; три мутації (зняти
утримання / поширити на всі загрози / утримувати й першу кризу) спіймано.

Тестова збірка (§7.9): при її відкаті 5 діб, вікні 1 і драбині 2 утримання
знімається на другу добу після бунту (раніше драбина починалася на першу);
дні бунтів не змінюються — `TestBuildTensionPaceTests` зелені.

### 9.3 Що вирішує власник

- **Варіант А (реалізовано):** утримання передвісника. Баланс колишній —
  криза кожні 30 діб, поки місто на Розпалі/Зламі; чесніше лише
  попередження.
- **Варіант Б (не реалізовано):** накопичувач кризи не копить під час
  відкату — після удару починає з нуля лише по завершенні відкату. Кризи
  рідші (30 діб відкату + 10–24 доби набору), і їхня частота сильніше
  залежить від того, наскільки висока Напруга. Це вже зміна балансу кампанії.
- Після затвердження: у `SETTLEMENT_LAYER.md` §3.2 до рядка «Передвісники»
  додати умову обраного варіанта.

## Поправка №10 — «Фракції отримують споживача, без повного стат-блоку» (2026-09-25)

> **Статус: ЧЕРНЕТКА, 2026-09-25.** Закриває Q-37/Q-38 з `docs/ledger/04-open-questions.md`.

Огляд усіх не-бойових механік (10 паралельних агентів) знайшов, що
`FactionRegistry.BandOf` (щабель довіри фракції) не має **жодного виклику
в усьому репозиторії**, крім власного оголошення — гравець рухає ставлення
фракцій діями ради, а гра на це ніяк не реагує.

**Рішення:** не будувати повний стат-блок фракцій (Опора/Достаток) і не
розводити окрему систему послуг (`FavorDefinition`, спроєктовану, але не
затверджену в `docs/AUDIT-LOOP.md`) — це роздуло б Епік 2 «Сторони» з M до
L (ризик N14, Q-37). Замість цього щабель довіри, який вже є, отримав
першого реального споживача:

- **`CityWorks.OrderDiplomacy`** відхиляє дію (`CouncilOrderResult.
  StandingTooLow`), якщо ціль — Hostile: простою дипломатією не купиш
  прощення в того, хто активно ворожий, довіру треба спершу підняти чимось
  іншим (Указ, квест, подія).
- **Сигнал (інваріант 4):** зміна щабля довіри від дій ради (Указ/
  Дипломатія/Облава) раніше логувалась НІМО — сигнал писав лише шлях
  наслідків квестів/данжу. Тепер `GameSession` звіряє полосу до/після
  виклику ради так само, як уже робить для наслідків, і пише
  `faction.standing_changed`.
- **Q-38 знімається як таке, що не настало:** окремої системи послуг так і
  не збудували, тож дублювати Указу/Дипломатії нема чим.

Побіжно той самий клас бага знайдено і в предметах:
`GameSession.GrantNamedItemById` визнавав лише жорстко зашитий id
`"scout_horn"` — готовий іменний предмет `AegisPlate` ніколи не видавався
гравцю. Виправлено генеричним пошуком через `DefaultItems.AllDefinitions()`.

Додано два нові квести з `docs/narrative/QUEST_CANDIDATES.md` (98-item
каталог, юридично перевірений) — `stone_soup` («Кам'яна юшка», Trade,
фракція Громада) і `market_toll` («Ринковий побір», за мотивами
«Рінконете і Кортадільйо» Сервантеса, Persuade/Intimidate, фракція Бояри
Тугара). Обидва — чорновий текст асистента (Поправка №7.3), не канон, до
правки власника. Кастинг імен НЕ переглядався окремо — обидва квести
використовують наявні три фракції, нових персонажів не заводять.
